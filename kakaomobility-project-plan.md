# Where-We-Go! — 프로젝트 계획서

출발지-목적지 이동시간 행렬 기반 모임 장소 비교 서비스
공간 데이터 수집 · 검수 · 갱신 파이프라인 위에 구축

> 카카오모빌리티 「백엔드 개발자(공간정보 시스템 개발)」 지원용 포트폴리오
> 작성 기준일: 2026-09-14 / 목표 기간: 3주
> **최종 갱신: 2026-09-19** — 2주차까지의 구현 결과를 반영했다.
> 계획과 달라진 곳은 ⚠️ 로 표시하고 **왜 달라졌는지**를 함께 적는다. 계획서를 결과에 맞춰
> 조용히 고치면 판단의 기록이 사라진다.

---

## 1. 프로젝트 개요

### 한 줄 정의
지인들이 방을 만들어 가고 싶은 장소를 모으면, 각자의 출발지에서 후보 장소까지의 이동시간(도보 + 지하철 + 버스)을 계산해 **비교표로 제공**하는 서비스. 그 아래에 **장소 데이터 수집·검수·갱신 파이프라인**을 직접 구축한다.

### 사용자 흐름
회원가입·로그인 → 방 생성(링크 공유) → 링크를 받은 사람이 참가 → 각자 가고 싶은 장소 북마크 → 각자 출발지 입력 → 후보 × 참여자 이동시간 행렬 확인. 순위는 시스템이 정하지 않고, 각 셀의 이동시간·환승·도보거리를 보고 사용자가 판단한다.

### 설계 원칙
| 원칙 | 이유 |
|---|---|
| **랭킹·추천 없음** | 시스템은 계산만 하고 판단은 사용자가 한다. 근거 없는 목적함수를 방어할 필요가 없음 |
| **경로탐색 직접 구현** | API로 대체하면 기성 서비스와 차별점이 0이 되고, JD 대응도 약해짐 |
| **실시간 동기화 없음** | JD 가점 0인데 시간을 가장 많이 먹는 구간. 폴링으로 대체 |
| **인증은 아이디/비밀번호만** ⚠️ 변경 | 원안은 "인증 없음"이었다 (2026-09-19 변경 — 아래 참조) |
| **프론트는 최소** | 지도 + 표. 그 이상 금지 |
| **데이터 파이프라인이 메인** | JD 담당업무 3개 중 2개가 여기서 채워짐 |
| **수단 추가에 열린 그래프** | 지하철+도보로 먼저 완결시키고, 버스는 같은 구조에 엣지 유형만 추가 |

> **인증을 넣기로 한 이유 (2026-09-19)**
>
> 원안은 "인증 없음 — 방 링크 UUID + 닉네임"이었다. JD 가점이 0이라는 판단은 지금도 유효하고,
> 실제로 이 결정은 **범위를 늘린다**. 그럼에도 넣은 것은 익명 구조의 대가가 설계 전반에 번졌기 때문이다.
>
> - 링크 소지가 곧 열람권이고, 입장 때 발급한 `memberId` 소지가 곧 본인 확인이었다.
>   그래서 "방 상태 응답에 타인의 `memberId`를 넣지 말 것"(구 §4 규약) 같은 제약이 필요했다.
>   **값 하나가 새면 남의 출발지를 바꿀 수 있는 구조**를 API 설계로 막고 있었던 셈이다
> - 재방문할 방법이 없었다. 링크를 잃으면 방도 잃는다
>
> 세션이 본인을 확인하면 이 제약이 통째로 사라진다. 범위는 늘었지만 **설계가 단순해지는
> 방향**이라 받아들였다. 대신 OAuth·소셜 로그인·이메일 인증·비밀번호 재설정은 넣지 않는다 —
> 거기서부터는 다시 JD와 무관한 시간이다.

### JD 대응 매핑
| JD | 대응 항목 |
|---|---|
| ① 공간정보 데이터 모델링 | raw/staging/serving 3계층, POI·검수이력·그래프(버전 관리)·방/북마크 스키마, 좌표계 설계(5174→5186 저장, 4326 직렬화) |
| ② 수집·검수·가공·배포 백엔드 시스템 | 인허가 데이터 적재 + 검수 규칙 + **회차 간 diff로 신규/유지/소멸 추적** + 조건부 요청 기반 증분 갱신 + 서비스 API |
| ③ 성능 개선 | 벌크 로드, 인덱스 전략, 반경 검색(GiST), 전문 검색(pg_trgm), 그래프 인메모리 캐싱 |
| (자격요건) CS 기본기 | 다중 출발지 최단경로(환승 포함) 직접 구현 |
| (우대) Java/Spring · PostgreSQL 설계 · GIS 개발 | 서비스 레이어 전체 · 3계층 스키마와 공간 인덱스 · PostGIS 공간 연산과 좌표계 변환 |

### 차별점 (위밋플레이스·야만나 대비)
위밋플레이스는 2019년 출시된 성숙한 서비스로, 참석자 위치와 교통편을 입력하면 이동시간·실시간 교통·배차간격을 고려해 중간지점을 산출한다. 겹치는 것은 결과 화면(UX)뿐이다.

- 탐색 공간이 다름: 그쪽은 지도 전체에서 중간지점을 찾고, 이쪽은 **사용자들이 모은 후보를 비교**한다
- 경로 계산과 POI 데이터를 외부에 의존하지 않고 **직접 구현·직접 적재**한다 — 이 프로젝트의 목적 자체가 그 아래층이다
- **출발 시각대별 비교**: 버스 구간 운행시간 실측 데이터가 시간대별이므로 `departureHour`를 바꿔가며 같은 행렬을 비교할 수 있다. 기성 서비스에 없는 축이다

> 면접 예상 질문 "위밋플레이스랑 뭐가 달라요?" 답변: 서비스로 경쟁하려는 게 아니라 그 밑의 데이터 처리와 경로 계산을 직접 구현한 프로젝트다. 문제 정의도 다르다.

---

## 2. 기술 스택

| 구분 | 선택 | 비고 |
|---|---|---|
| 백엔드 | Spring Boot 4 + JDK 25 | 학습 중인 스택. 우대사항 직접 대응 |
| DB 접근 | **JdbcTemplate** | JPA/Hibernate Spatial 금지 — geometry 매핑에서 며칠 태움. 공간 연산을 SQL 로 직접 쓴다 |
| DB | PostgreSQL + PostGIS | GiST 인덱스, pg_trgm, 좌표계 변환(5174→5186), 공간 조인 |
| 마이그레이션 | Flyway | 스키마 변경 이력이 곧 설계 증거 |
| ETL | Python (pandas, GeoPandas, pyproj) | 기존 강점. 여기서 시간 낭비 금지. **venv 필수** — 시스템 파이썬에 설치하지 않는다 |
| 좌표 변환 | pyproj + PostGIS, **proj4 문자열 명시 고정** | EPSG 코드에 의존하면 PROJ 버전·엔진별로 결과가 갈린다 (§6 D0) |
| 프론트 | **Vite + 바닐라 JS** | 원안은 React+TS 였다. 화면이 폼 둘·지도·표뿐이라 프레임워크를 넣으면 "의존성 없이 클론하면 바로 돈다"는 강점만 희석된다. 라우팅은 해시 라우터로 직접 |
| 지도 | **카카오맵 JS SDK** | 배경지도 표시 용도. 키는 서버가 `/client-config` 로 넘긴다(저장소에 넣지 않는다) |
| 인프라 | Docker Compose (postgis + flyway + app) | 재현 가능성 확보. 마이그레이션은 별도 일회성 컨테이너가 적용한다 |
| 형상관리 | GitHub 공개 레포 `Where-we-go` | README가 승부처 |
| IDE | 서버 IntelliJ / ETL·프론트 VS Code | SQL 문자열 자동완성·Database 툴 |

**인증**: Spring Security 세션 + BCrypt. 아이디/비밀번호만 (§1 변경 사유 참조).

쓰지 않는 것: OAuth·소셜 로그인·이메일 인증·비밀번호 재설정, WebSocket, pgRouting, 외부 길찾기 API(대조 벤치마크 용도 제외).

> **외부 API 키·활용신청이 필요한 구성요소가 없다.** 배경지도만 키가 필요하고, 나머지는 전부 파일이다.

---

## 3. 사용 데이터

| # | 데이터 | 출처 | 용도 | 갱신 | 확인된 사항 / 주의점 |
|---|---|---|---|---|---|
| 1 | 일반음식점·휴게음식점 인허가 | LOCALDATA (행정안전부) | **POI 본체 + 영업상태** | 전체분 + 일 변동분 | **39컬럼 / 서울 전체 685,718행** (일반 538,576 + 휴게 147,142). 상호명·주소·좌표·업태구분명·영업상태·폐업일자·인허가일자. **인코딩 CP949(EUC-KR)**. 좌표는 `좌표정보(X)/(Y)`, 좌표계 **EPSG:5174**. `file.localdata.go.kr`는 **Referer 헤더 필요(없으면 403)** |
| 2 | 도보망 | OpenStreetMap | 도보 구간 | - | OSMnx `network_type='walk'`. **서울 전역** 165,050 노드 / 472,866 엣지 (구역을 좁힐 필요가 없었다) |
| 3 | **도시철도 열차운행시각표** | 공공데이터포털 (서울교통공사) | **역간 소요시간 + 시간대별 배차** | - | **532,832행 / 1~9호선 전부.** `LINE, SI_ID, STATION_NM, WEEKTAG(DAY/SAT/END), INOUTTAG(UP/DOWN/IN/OUT), GUBHANG(급행), TRAIN_NO, STT, EDT, ST_STT_NM, ED_STT_NM`. **인코딩 UTF-8 BOM** — 다른 파일과 다르다. `STT` 최대 `25:14:00`(24시 초과 표기) |
| 4 | 지하철 환승역거리·소요시간 | 서울교통공사 | 9호선 환승 추정의 바탕 | - | `호선, 환승역명, 환승노선, 환승거리(m), 환승소요시간(mm:ss)`. **소요시간은 잰 값이 아니라 `거리 ÷ 1.2m/s`** 다(140행 전부 ±1초) |
| 4-1 | **수도권 도시철도 환승 데이터** (OA-22521) | 서울교통공사 | **환승 도보시간 측정값** | - | 내린 칸 → 탈 칸 단위 측정값. 1~8호선 99쌍. **9호선 1단계는 전부 10:00 자리값** — §8 참조 |
| 5 | 지하철 역사마스터 | 서울 열린데이터광장 CSV | 역 좌표 | - | **위도·경도(EPSG:4326)** 784행. 3번과 **역명 매핑 필요** (아래) |
| 6 | 지하철 역간거리·소요시간 | 서울 열린데이터광장 (서울교통공사) | **역간거리(km)만** | - | 1~8호선 279구간. 소요시간은 3번(실측 시각표)이 더 정확하고 넓으므로 **거리가 필요할 때만 쓴다** |
| — | 열차운행현황 | 서울교통공사 | (대조용) | - | 13행 요약표. 호선별 RH/NH 시격, 표정속도. **3번 산출값의 타당성 대조에 쓴다** |
| 7 | 버스 노선마스터 | 서울 열린데이터광장 CSV | 노선 식별·유형 | - | `노선_ID`, `노선_명칭`, `노선_유형`, `거리`. 1,539노선 |
| 8 | 버스 구간별 평균 운행시간 | 서울 열린데이터광장 CSV | 버스 승차 구간 | - | **인접 정류장 구간 + 시간대별(00~23시, 24개) 실측.** 574,953행 / 7일치(월~일). 구간 중앙 105초. **`출발/도착_정류장_순서` 컬럼으로 노선별 정류장 순번이 직접 제공된다** |
| 9 | 버스 정류장마스터 | 서울 열린데이터광장 CSV | 정류소 좌표 | - | **위도·경도(EPSG:4326)**, 11,494건. 서울시 정류장만 수록 |
| 10 | 버스노선 기본정보 | 서울 열린데이터광장 XLSX | **배차간격(실측)** | - | **평일/토요일/공휴일 3시트** × 822행. `배차간격`, `최소배차`, `최대배차`, `첫차시간`, `막차시간`, `인가대수`. 키는 `노선번호` |
| 11 | 버스노선 ID정보 | 서울 열린데이터광장 XLSX | 조인 키 | - | `노선명` ↔ `ROUTEID` 723행. 10번(노선번호)과 8번(노선_ID)을 잇는다 |
| 12 | 카카오모빌리티 길찾기 API | 카카오모빌리티 디벨로퍼스 | (선택) 대조 벤치마크 | - | 다중 출발지 길찾기 존재(최대 30개). 권한 신청 절차 있음. **안 써도 프로젝트가 완결된다** |

> **전 데이터가 파일이다.** 외부 API 키·활용신청 없이 클론 후 바로 실행할 수 있다. 심사자가 직접 돌려볼 수 있다는 뜻이며, README 최상단에 쓴다.

### 인코딩 — 파일마다 다르다
| 파일 | 인코딩 |
|---|---|
| 인허가(POI), 버스 전 파일, 지하철 역사마스터·환승·역간거리 | **CP949 (EUC-KR)** |
| 도시철도 열차운행시각표 | **UTF-8 BOM** |

적재기는 인코딩을 파일별 설정으로 받고, 자동 감지에 기대지 않는다.

### 단일 소스 선택의 이유
초안은 상가(상권)정보를 POI 본체로, 인허가를 대조 검수용으로 쓰는 2소스 구조였다.
그러나 인허가 데이터에 사업자등록번호가 없어 **상호명 매칭만 가능**했고, 매칭률이 낮으면
검수 축 전체가 무너지는 구조적 리스크가 있었다. 인허가 단일 소스로 통일해 이 리스크를 제거하고,
검수는 **단일 소스 내부 규칙 + 회차 간 diff**로 구성한다(§6 2단계).

부수 효과로 POI가 음식점·카페로 한정되는데, 모임 장소 서비스의 후보로는 오히려 적합하다.

### 이동수단 범위: 도보 + 지하철 + 버스
- 차량은 제외 — 표준노드링크가 왕복 2차로 이상 도로만 구축 대상이라 골목 목적지 처리가 안 되고, 서울 지인 모임에서 전원 차량 가정은 비현실적
- **완성 순서는 지하철+도보 → 버스.** 지하철+도보만으로도 프로젝트가 완결되도록 만들고, 버스는 같은 그래프 구조에 노드·엣지를 추가하는 방식으로 확장한다. 일정이 밀리면 버스가 먼저 빠진다

### 좌표계 — 원본이 둘, 저장은 하나 ✅ 검증 완료 (2026-09-14)

| 데이터 | 원본 좌표계 | 저장 |
|---|---|---|
| POI (인허가) | **EPSG:5174** 계열 (`좌표정보(X)/(Y)`) | → 5186 |
| 버스 정류장마스터 | **EPSG:4326** (위도/경도) | → 5186 |
| 지하철 역사마스터 | **EPSG:4326** (위도/경도) | → 5186 |

두 좌표계를 5186으로 통일해도 **정합이 깨지지 않는다**는 것을 실측으로 확인했다.
구현: [`etl/verify_crs_alignment.py`](etl/verify_crs_alignment.py) · 리포트 `etl/out/crs_alignment.json`

POI 20,000건에서 **가장 가까운 버스 정류장까지의 거리**를 5186 평면에서 잰 결과:

| POI 좌표계 해석 | 중앙 | p95 | 100m 이내 |
|---|---|---|---|
| **EPSG:5174 (채택)** | **80.5m** | 202.1m | **62.6%** |
| EPSG:2097 (보정 미적용) | 104.7m | 250.3m | 47.3% |
| *[음성대조군] 5186으로 오인* | 88,259.3m | 95,418.9m | 0.0% |

서울 시가지에서 음식점 → 최근접 정류장 80m는 타당한 값이다. **좌표계를 잘못 고르면 분포가 통째로 밀린다** — 음성 대조군이 88km로 튀는 것이 그 증거다. 앵커를 손으로 넣지 않고 정류장 11,494개를 기준으로 삼았으므로, §6 D0 판정을 **독립 데이터로 재확인**한 셈이다.

> 4326 → 5186은 같은 datum 안에서 투영만 바꾸는 것이라 손실이 없다. 어려운 쪽은 5174의 Bessel → GRS80 **datum 변환**이고, 그건 §6 D0에서 이미 검증했다.

### 버스·지하철 소요시간 — 전부 실측
버스 구간 운행시간과 배차간격을 서울시 CSV에서 실측으로 확보했다. **추정 모델을 쓰지 않는다.**

- 승차 구간: 구간별 평균 운행시간 **시간대별(00~23시)** 값. 인접 정류장 단위이며 중앙 105초
- **정류장 순번은 구간 데이터에 직접 들어 있다** (`출발_정류장_순서` / `도착_정류장_순서`). 경유순번 데이터를 따로 받을 필요가 없다
- 환승 도보시간은 1~8호선이 측정값(4-1), 9호선은 거리값(4)을 측정값 기준으로 보정한 추정치다(§8). 폴백(180초) 비율은 `/admin/graph-stats`에 노출하며 현재 0이다
- 버스 대기: **배차간격 ÷ 2 (실측)**. 평일/토요일/공휴일 구분이 있으므로 `departureHour`와 함께 요일 축도 쓸 수 있다
- 지하철 대기: **배차간격 ÷ 2 (시각표에서 산출)**. 버스와 같은 공식이므로 설명이 일관되고, 어느 쪽도 0이 아니라 경로 선택이 왜곡되지 않는다

#### 버스 배차 조인 ✅ 확인 완료 (2026-09-15)
배차 파일의 키(`노선번호`)와 구간 데이터의 키(`노선_ID`)가 다르다. 노선ID정보가 둘을 잇는다.

```
노선번호 '0017' ──[노선ID정보]──▶ ROUTEID 100100124 ──▶ 구간 데이터 노선_ID
```

- 배차 → ID정보 매칭 **822/822**
- **구간 데이터의 서울 노선 693개 전부 배차 확보 (100%)**
- 노선마스터의 `노선_명칭`으로 잇는 경로(820/822)도 있으나, ID정보 경로가 더 깨끗하다

#### 지하철 — 시각표 하나로 역간 소요시간과 배차를 모두 산출 ✅ 확인 완료 (2026-09-15)

`TRAIN_NO`(열차번호)가 있어 **같은 열차를 추적할 수 있다.** 이것이 없으면 역간 소요시간 산출이 불가능하다.

**① 역간 소요시간** — 같은 열차의 연속 역
```
(LINE, INOUTTAG, TRAIN_NO)로 묶고 시각순 정렬 → 다음 역 STT − 현재 역 EDT
```
- 주중 154,505구간 중 **유효 98.1%**
- 주행시간 중앙 **90초** (5% 60초 / 95% 180초) — 타당한 분포
- 고유 역간 구간 993개, **9호선 102구간 포함**
- **1호선은 265구간** — 시각표의 1호선은 서울교통공사 구간(서울역~청량리 10역)이 아니라 코레일 직결 전체(양주~인천)다. 기존 역간거리 파일보다 커버리지가 넓다

**② 시간대별 배차** — 같은 역·같은 방향의 연속 열차
```
(SI_ID, INOUTTAG)로 묶고 STT 정렬 → 연속 차이
```
주중 배차 중앙(분):

| 호선 | 8시 | 19시 | 22시 |
|---|---|---|---|
| 2호선 | 3.0 | 3.5 | 7.0 |
| 7호선 | 4.0 | 10.0 | 15.0 |
| 9호선 | 4.8 | 6.8 | 11.0 |

> **⚠ 그룹 기준을 `ED_STT_NM`(종착지)까지 내리면 배차가 2~4배 부풀려진다.**
> 1호선 8시대가 종착지별로는 13.0분이 나오지만 `(역, 방향)`만으로는 4.5분이고, 열차운행현황의 실측 시격은 3분이다.
> 인천행·신창행·광운대행이 갈려서 생기는 과대평가이며, 실제로 시청역 가는 승객은 아무 하행이나 탄다.
> **`(SI_ID, INOUTTAG, 시간대)`로 묶는다.** 분기 노선에서 실제 대기가 더 길 수 있다는 한계는 README에 명시한다
> (정확히 다루려면 경로 의존 모델이 필요하고, 그건 이 프로젝트 범위를 넘는다).

**③ 산출값 타당성 대조** — 열차운행현황(13행 요약표)의 호선별 RH/NH 시격과 나란히 놓아 확인한다.
8시대 기준 시각표 산출값이 운행현황보다 1~1.5분 큰데, 중앙값/평균 차이와 급행 포함 여부에서 온다.

**④ 파싱 주의**
- `STT`가 `25:14:00`까지 있다 (24시=익일 0시). 문자열을 그대로 `time`으로 파싱하면 깨진다 — **자정 이후 초 단위로 환산**한다
- `STT`/`EDT` 결측 약 15,000행 (시·종착역) → 서로 채운 뒤 남는 것만 버린다

**⑤ 역명 ↔ 좌표 조인 — 노선명 매핑이 필요하다**
역사마스터가 1호선 바깥 구간을 **경부선·경인선·경원선**, 4호선을 **안산선·과천선**, 3호선을 **일산선**,
8호선을 **별내선** 등 별도 노선명으로 담고 있다. 매핑 없이는 70.6%만 붙고, 매핑 후 **98.0%**가 된다.
남는 9건(`회기` `창동` `까치산` `자양` `진접` `오남` `별내별가람` `평택지제`)은 수동 매핑 테이블로 처리한다.

### 버스 정류장 좌표 커버리지
구간 데이터에는 경기·인천 노선이 섞여 있고, 정류장마스터는 서울시 정류장만 담고 있다.

| 노선 유형 | 노선 수 | 정류장 | 좌표 보유 |
|---|---|---|---|
| 마을 | 247 | 5,641 | 99.2% |
| 공항 | 38 | 931 | 95.3% |
| 지선 | 239 | 6,401 | 89.3% |
| 간선 | 152 | 5,230 | 77.6% |
| **서울 노선 소계** | **692** | **12,768** | **86.2%** |
| 광역 | 13 | 414 | 30.9% |
| 인천 | 36 | 1,034 | 15.0% |
| 경기 | 588 | 13,181 | 12.7% |

- **경기·인천·광역 노선은 제외한다.** 좌표가 없어 그래프에 넣을 수 없고, 서울 내 모임 장소 탐색에 기여도 적다
- 서울 노선에서도 좌표 없는 정류장(13.8%)이 낀 구간은 엣지에서 빠진다. **제외 비율을 `/admin/graph-stats`에 노출**한다
- 대상 구역은 서울 전역이다. 영향 범위는 위 수치 그대로다

### 시간대별 가중치 처리
버스 구간 운행시간이 1시간 단위로 다르므로 정적 엣지 가중치로는 데이터를 쓸 수 없다.

- 버스 엣지는 가중치를 **시간대 배열(24개)**로 보관하고, 탐색 시점에 `weights[departureHour]`를 읽는다. 그래프 빌드는 한 번만 한다
- 도보·지하철 엣지는 시간 불변이므로 단일 값을 유지하되 같은 인터페이스로 감싼다
- 요청 파라미터 `departureHour`(기본 19)로 노출 — **"19시 출발 vs 21시 출발 비교"가 위밋플레이스에 없는 차별점**이 된다
- 단순화: 경유 중 시간 경과는 반영하지 않는다(출발 시각 기준 고정). README에 명시한다

---

## 4. API 명세

### 공통 규약
- Base URL `/api/v1`, JSON, UTF-8
- **요청·응답 좌표는 WGS84(EPSG:4326)** `lng`/`lat`, 시간은 초, 거리는 미터
- **내부 저장은 EPSG:5186**(§7). 4326 변환은 API 직렬화 계층에서만 수행한다
- **인증은 세션 쿠키.** 로그인하면 `JSESSIONID`(HttpOnly, SameSite=Lax)가 발급되고, 이후 요청은 그것으로 본인이 확인된다
  - **요청에 사용자 식별자를 싣지 않는다.** 실어 보내면 그 값을 바꿔 남 행세를 할 수 있는지부터 따져야 한다. 본인은 언제나 세션에서 온다
  - 원안에 있던 "타인의 `memberId`를 노출하지 말 것"이라는 제약은 **사라졌다.** 식별자 소지가 곧 권한이던 구조에서 필요했던 것이고, 지금은 알아도 아무것도 못 한다. 오히려 행렬의 행을 가리키려면 `memberId`가 필요하다
  - **CSRF 토큰을 쓴다.** 세션 쿠키는 브라우저가 알아서 붙이므로 남의 사이트에서 우리 API를 불러도 쿠키가 함께 간다. `GET /auth/csrf`로 받은 값을 `X-XSRF-TOKEN` 헤더에 실어 보낸다
- 장소 검색·경로 조회는 **로그인 없이** 열려 있다. 방을 만들기 전에 둘러볼 수 있어야 하고, 개인 정보가 실리지 않는다
- 에러는 `message`(+ 입력 오류면 칸별 `fields`) 구조. 주요 상태: 400(입력), 401(미로그인), 403(참가자 아님·남의 것), 404(없는 방·장소), 409(중복 가입값·중복 북마크·출발지 스냅 실패), 503(그래프 미적재)

### 4.0 계정

**POST `/auth/signup`** — 가입. 요청: `nickname`, `loginId`, `password`, `passwordConfirm`. 응답 201 + `{userId, loginId, nickname}`.
- `loginId`는 영문·숫자·밑줄 4~20자, **소문자로 정규화**해 저장한다. 대소문자만 다른 계정이 생기면 안 된다
- 닉네임은 2~20자. 중복은 **대소문자를 무시하고** 막는다(`lower(nickname)` 유니크 인덱스) — 'Hong'과 'hong'이 갈리면 방 화면에서 누가 누구인지 구분할 수 없다. 보이는 이름은 입력한 대로 보관한다
- 비밀번호는 8~64자. 위쪽 한계는 **BCrypt가 72바이트까지만 보기 때문**이다. 그보다 긴 부분은 조용히 잘린다
- `passwordConfirm`은 **서버도 본다.** 화면 검사는 사용자 편의지 검증이 아니고, API는 화면 없이도 호출된다
- **가입 직후 바로 로그인 상태가 된다.** 방금 정한 비밀번호를 한 번 더 치게 할 이유가 없다
- 중복이면 409 + 어느 칸이 문제인지(`fields`). 가입에서는 알려줄 수밖에 없다 — 아니면 가입 자체가 불가능하다

**POST `/auth/login`** — 요청: `loginId`, `password`. 아이디의 대소문자는 가리지 않는다.
> 실패는 **아이디가 없는 것인지 비밀번호가 틀린 것인지 구분해 알려주지 않는다.** 구분해 주면 어떤 아이디가 존재하는지 확인하는 통로가 된다. 두 응답이 바이트 단위로 같은지 테스트로 고정한다.

**POST `/auth/logout`** — 세션을 무효화한다. 204.

**GET `/auth/me`** — 현재 로그인한 계정. 미로그인이면 401(JSON). **로그인 폼으로 리다이렉트하지 않는다.**

**GET `/auth/csrf`** — CSRF 토큰 발급. 프론트가 첫 화면에서 한 번 부른다.
> 토큰은 **누군가 요구해야 만들어진다.** 이 엔드포인트가 없으면 쿠키가 한 번도 내려가지 않아 브라우저에서는 모든 POST가 403이 된다. 실제로 그 상태였고, 테스트가 토큰을 밀어 넣는 방식이라 드러나지 않았다(§6 「테스트가 통과하는데 동작하지 않았던 것」).

**비밀번호는 BCrypt 해시로만 저장한다.** DB에도 로그에도 응답에도 평문은 없다. 해시가 응답에 섞여 나가지 않는지도 테스트로 고정한다.

### 4.1 장소

**GET `/places/search`** — 키워드 POI 검색. 자체 DB만 조회, 외부 API 미호출.

| 파라미터 | 필수 | 설명 |
|---|---|---|
| `q` | O | 상호명 부분 일치 검색어. `LIKE '%q%'`는 B-tree를 못 타므로 **pg_trgm GIN 인덱스** 사용 |
| `lng`, `lat` | X | 주어지면 거리순 정렬 |
| `radius` | X | 반경(m), 기본 2000, 최대 10000 |
| `category` | X | **업태구분 코드**(정규화값). 원본은 인허가의 업태구분명 문자열이며 `category_map`으로 정규화한다 |
| `includeClosed` | X | 폐업 항목 포함 여부, 기본 false. 기본 질의는 `WHERE status='ACTIVE'` |

응답 항목: `poiId`, `name`, `category`(정규화 코드 + 원본 업태구분명), 도로명주소, 좌표, `status`(ACTIVE/CLOSED), `closedDate`, 검수 회차와 확인일, (좌표 준 경우) 거리.

**GET `/places/nearby`** — 좌표 기준 반경 검색. ST_DWithin + GiST 성능 측정의 대상 엔드포인트. 파라미터: `lng`·`lat`(필수), `radius`, `category`. 응답 형식은 search와 동일.
> 내부 질의는 5186 미터 단위로 수행한다(도 단위 오판과 `::geography` 캐스팅으로 인한 인덱스 미사용을 피한다).

**GET `/places/{poiId}`** — 단건 상세. 기본 정보에 더해 데이터 출처(원본 스냅샷 기준일), 검수 결과(적용 규칙·판정·severity·확인일), 등장 회차(first/last seen run), 영업상태 변경 이력을 포함한다.

### 4.2 방

**POST `/rooms`** — 방 생성. 요청: `title`. 응답: `roomId`(UUID). 닉네임은 계정에 있으므로 받지 않는다. **만든 사람은 자동으로 참가한다** — 따로 들어가게 할 이유가 없다.

**GET `/rooms`** — 내가 참가한 방 목록. 계정이 생기면서 가능해진 화면이다(익명 구조에서는 링크를 잃으면 방도 잃었다).

**POST `/rooms/{roomId}/members`** — 참가. 본문 없음. **멱등이다** — 다시 눌러도 참가자가 늘지 않는다.

**GET `/rooms/{roomId}`** — 방 상태 전체(참가자 목록·출발지·북마크 목록·`matrixReady`). 프론트는 이 엔드포인트만 폴링한다.
- **참가자가 아니면 403.** 방 링크는 초대장이지 열람권이 아니다 — 링크를 받은 사람은 참가한 뒤에 본다
- 참가자 항목: `memberId`, 닉네임, `isMe`, 출발지(라벨·좌표·스냅 거리·`originStale`)
- `matrixReady`는 **"계산 가능 여부"**(출발지 1명 이상 + 북마크 1개 이상)를 뜻한다. 행렬 계산은 동기이므로 "계산 완료 여부"가 아니다

**PUT `/rooms/{roomId}/members/me/origin`** — 자기 출발지 설정. 좌표(`lng`·`lat`·`label`) 또는 지하철역 이름(`station`). 응답에 **스냅 결과(그래프 노드, 스냅 거리, `graphBuildId`)를 항상 포함** — 스냅 거리가 크면 계산 신뢰도가 낮다는 신호이므로 숨기지 않는다. 붙일 곳이 없으면 409(서울 밖 좌표).
> 저장된 `graphBuildId`가 활성 빌드와 다르면 방 상태의 `originStale`이 참이 되고, 행렬 계산 시 **재스냅 후 진행**한다(그래프 재빌드로 node_id가 무효화되는 것을 방어).

### 4.3 북마크

**POST `/rooms/{roomId}/bookmarks`** — 후보 추가. 검색 결과 선택(`poiId`) 또는 지도 클릭 직접 입력(`name` + 좌표) 두 방식. 응답: `bookmarkId`.
> `poiId` 선택 시에도 **추가 시점의 이름·좌표를 북마크 행에 스냅샷으로 복사**한다. 이후 해당 POI가 폐업 처리되거나 원본에서 사라져도 방 화면과 이미 계산된 행렬이 깨지지 않는다.

**DELETE `/rooms/{roomId}/bookmarks/{bookmarkId}`** — 담은 사람만. 204. 남의 것이면 403, 없으면 404 — 화면이 두 경우를 다르게 설명할 수 있어야 한다.

> 같은 POI 를 두 번 담으면 409다. 부분 유니크 인덱스(`WHERE poi_id IS NOT NULL`)로 막으므로 지도 클릭 북마크에는 적용되지 않는다 — 같은 자리를 다른 이름으로 두 번 찍는 것은 막을 이유가 없다.

### 4.4 이동시간 행렬 — 핵심 엔드포인트

**POST `/rooms/{roomId}/matrix`** — 모든 멤버 출발지 × 모든 북마크의 이동시간 행렬 계산.

**동기 계산 후 결과를 반환한다.** 2개 구 규모·출발지 5~10명이면 one-to-many 다익스트라 수 회로 충분히 수초 내다. 비동기 잡·폴링을 두지 않는다. 방 단위 동시 계산은 1건으로 제한한다(초과 시 429) — 공개 URL이므로 최소한의 남용 방어가 필요하다.

요청 옵션(전부 선택):

| 옵션 | 기본 | 설명 |
|---|---|---|
| `mode` | TRANSIT | TRANSIT(도보+지하철+버스) / SUBWAY_ONLY / WALK |
| `departureHour` | 19 | 출발 시각대(0~23). **버스·지하철 엣지 가중치와 대기시간을 이 시간대 값으로 선택**한다 |
| `dayType` | DAY | DAY(주중) / SAT(토요일) / END(일요일·공휴일). 버스·지하철 양쪽 데이터에 이 구분이 있다 |
| `transferPenalty` | 측정값 | 환승쌍별 도보시간 — 1~8호선 측정값, 9호선 보정 추정치. 두 파일 어디에도 없는 쌍만 180초 폴백(현재 0쌍) |
| `maxWalkDistance` | 1200m | 진출입 도보 허용 최대 거리 |
| `walkSpeed` | 1.2 m/s | 도보 속도 |

응답 구성:

| 블록 | 내용 |
|---|---|
| `origins` / `destinations` | 행렬의 축 (멤버 목록, 북마크 목록) |
| `cells` | 셀별 `duration`, `walkDistance`, `transfers`, `waitTime`, 이용 수단 조합, `reachable`, (실패 시) `unreachableReason` |
| `stats` | `dijkstraRuns`, `expandedNodes`, `elapsedMs`, `graphSource`(CACHE/DB), `graphBuildId`, `transferPenaltyFallbackRatio` |

- **목적지 스냅 실패는 해당 셀만 `reachable=false` + 사유 코드**로 처리하고 행렬 전체는 성공으로 반환한다. 출발지 스냅 실패만 409다
- `departureHour`를 바꿔 같은 방을 두 번 계산하면 **버스·지하철 셀 모두** `duration`이 달라진다 — 이것이 시간대 비교 기능이다

> **`stats` 블록이 성능 서사를 담는다.** `dijkstraRuns`가 출발지 수와 같고 목적지 수와 무관하다는 것이 one-to-many 탐색의 증거이고, `expandedNodes`·`elapsedMs`가 최적화 전후 비교 수치가 된다. 랭킹은 제공하지 않는다 — 정렬과 판단은 사용자의 몫.

**GET `/rooms/{roomId}/routes`** — 셀 하나를 눌렀을 때의 단건 경로 상세.
파라미터: `originMemberId`, `bookmarkId`, `mode`, `departureHour`. 응답: 총 소요시간, 구간(leg) 목록 — 도보/지하철/버스/환승 각각의 구간별 소요시간·거리·노선명·정차 수 — 그리고 지도 표시용 LineString 좌표열(4326).

> 좌표를 다시 받아 재계산하지 않고 **행렬과 같은 입력·같은 그래프 빌드에서 재현**한다. 좌표 재입력 방식은 동률 경로에서 행렬 셀과 상세가 어긋날 수 있다.

### 4.5 운영·검수 (관리자 전용)

심사자가 코드를 읽지 않아도 파이프라인 동작을 확인할 수 있게 한다. 화면은 `/#/admin`.

> **처음 계획은 로그인 없이 공개였으나 관리자 전용으로 바꿨다.** 검수 결과에는 개별 업소의 인허가번호·주소가 판정 사유와 함께 나오고, 방 링크만 알면 누구나 들어오는 서비스라 공개 범위를 좁힐 이유가 있었다. 관리자는 환경변수 `WWG_ADMIN_LOGIN_IDS`(쉼표 구분 아이디)로 정한다 — 역할 테이블을 두지 않은 것은 관리자가 두 명이고 바뀔 일이 드물어서다. 비로그인은 401, 일반 사용자는 403.

| 엔드포인트 | 내용 |
|---|---|
| GET `/admin/ingest-runs` | 적재 회차 목록 — 회차별 상태(SUCCESS/FAILED/SKIPPED), 원본/유효/거절 건수, **신규/유지/소멸/폐업전환 건수**, 소요시간. **README에 붙일 표가 이 응답 그대로** |
| GET `/admin/validation-summary` | 최신(또는 지정) 회차의 검수 규칙별 집계 (severity별 ERROR/WARN 분리) |
| GET `/admin/validation-results` | 개별 판정 레코드 (규칙·회차·severity 필터, 페이징 — 기본 50, 최대 500) |
| GET `/admin/graph-stats` | 그래프 빌드 결과 — `buildId`, 활성 여부, 수단별 노드·엣지 수, 환승 엣지 수, **환승 페널티 폴백 비율**, 고립 노드 수, 빌드 소요시간 |

### 엔드포인트 요약

| Method | Path | 용도 | 로그인 |
|---|---|---|---|
| POST | `/auth/signup` · `/auth/login` · `/auth/logout` | 가입·로그인·로그아웃 | — |
| GET | `/auth/me` · `/auth/csrf` | 내 정보 · CSRF 토큰 | me만 필요 |
| GET | `/places/search` · `/places/nearby` · `/places/{poiId}` | POI 검색·반경 조회·상세 | 불필요 |
| GET | `/routes` | 단건 경로 (좌표 또는 역 이름) | 불필요 |
| GET | `/graph` · POST `/graph/reload` | 올려둔 그래프 상태 · 재적재 | reload만 필요 |
| POST | `/rooms` | 방 생성 | 필요 |
| GET | `/rooms` | 내가 참가한 방 목록 | 필요 |
| GET | `/rooms/{roomId}` | 방 상태(폴링 대상) | 참가자만 |
| POST | `/rooms/{roomId}/members` | 참가 (멱등) | 필요 |
| PUT | `/rooms/{roomId}/members/me/origin` | 출발지 설정 | 참가자만 |
| POST / DELETE | `/rooms/{roomId}/bookmarks[/{id}]` | 후보 추가·삭제 | 참가자만 |
| **POST** | **`/rooms/{roomId}/matrix`** | **이동시간 행렬 계산 (동기)** | 참가자만 |
| GET | `/rooms/{roomId}/routes` | 행렬 셀의 경로 상세 | 참가자만 |
| GET | `/admin/*` | 적재 회차·검수 집계·그래프 통계 | **관리자만** (§4.5) |

---

## 5. 디렉토리 구조 (상위 수준)

| 경로 | 역할 |
|---|---|
| `etl/ingest/` | LOCALDATA 인허가·행정경계·교통 CSV 적재. 단일 진입점(`python -m ingest --snapshot …`), 조건부 요청, 회차 diff |
| `etl/validate/rules/` | 검수 규칙 — SQL 파일 1개당 1규칙 |
| `etl/build_graph.py` | 도보망 + 지하철 + 버스 → 그래프 스냅샷 빌드 |
| `etl/route.py` | 경로탐색 **참조 구현**. 서버와 같은 답을 내는지 대조하는 기준 |
| `server/ … /account` | 가입·로그인·현재 사용자 |
| `server/ … /config` | Spring Security(세션·CSRF), CORS, 카카오 키 전달 |
| `server/ … /place` | POI 검색·상세 |
| `server/ … /room` | 방·멤버·북마크 |
| `server/ … /graph` | 메모리 그래프(CSR), DB 적재, 캐시 |
| `server/ … /routing` | 다익스트라, 경로 조립, 좌표 스냅 |
| `server/ … /matrix` | 다중 출발지 행렬 계산 |
| `server/ … /admin` | 적재·검수·그래프 조회 |
| `server/ … /resources/db/migration` | Flyway 마이그레이션 |
| `web/` | Vite + 바닐라 JS — 로그인, 방, 지도, 행렬 표 |
| `docs/` | perf.md — 성능 측정 결과 (`./gradlew perfTest` 산출물) |

---

## 6. 작업 순서 (3주)

원칙: **얇은 수직 슬라이스를 1주차에 완성**한다. 아래층을 전부 쌓고 위로 올라가면 중간에 막혔을 때 보여줄 것이 없다. 첫 스크린샷을 1주차 전반에 확보한다.

| 시점 | 작업 | 상세 / 산출물 |
|---|---|---|
| **D0** | 사전 검증 | ✅ 좌표계 판정 / ✅ 좌표 정합(POI↔정류장) / ✅ 버스 데이터 구조·배차 조인 / ✅ 지하철 시각표 산출 검증 — **아래 참조.** 남은 것: ⬜ `Last-Modified`·`ETag` 확인. ✅ OSM 추출은 **서울 전역**으로 해소(구역 한정 불필요) |
| **1주 전반** ✅ | 스키마 + 적재 + 첫 화면 | ✅ Docker Compose + Flyway, raw/staging/serving 3계층, **인허가 2종 서울 전체 적재**(68만 행 2분 28초). ✅ `/places/nearby` + 지도에 점 찍는 최소 프론트 |
| **1주 후반** | 검수 + 증분 갱신 | 검수 규칙 SQL 7종. **회차 diff**(신규/유지/소멸/폐업전환). 조건부 요청 SKIPPED(+헤더 없으면 해시). 멱등성 확인. **일일 자동 실행 등록** → 이후 2주간 회차가 저절로 쌓인다 |
| **2주 전반** ✅ | 그래프 빌드 (도보+지하철**+버스**) | ✅ OSM 보행망(**서울 전역**) + 지하철 + **버스**를 하나의 그래프로. 노선별 역 노드 복제로 환승 엣지화. 시각표에서 역간 소요시간·시간대별 배차 산출, 환승 실측값 반영, `station_alias` 매핑, `build_id` 버저닝 → 첫 빌드 WALK 472,866 / RIDE 37,410 / TRANSFER 108, 107초. 버스를 3주 후반에서 당겨 왔다. **마감 시점(빌드 #17): 노드 213,661 / 엣지 607,183 — WALK 472,866 · RIDE 36,987 · BOARD·ALIGHT 각 37,168 · ACCESS 22,614 · TRANSFER 380, 고립 노드 0, 폴백 0, 약 150초** |
| **2주 후반** ✅ | 서버 골격 + 경로탐색 | ✅ Spring Boot. 장소·방·북마크 API. 다익스트라(CSR + long 힙) + 인메모리 캐시 + 스냅. 단건 경로 동작. **추가: 계정·로그인**(§1 변경 사유) |
| **3주 전반** ✅ | 프론트 + 행렬 계산 | ✅ 화면 뼈대(해시 라우터·CSRF 래퍼) + 로그인·방·지도 화면. ✅ 출발지별 one-to-many 탐색, `stats` 채우기, 행렬 표 화면. ✅ 셀 → 경로 상세(지도에 경로 선 + 구간 타임라인). ✅ 장소 카드(`/places/{poiId}`)와 북마크 토글. ⬜ 후보 반경 사전 축소는 넣지 않았다. 목표를 다 확정하면 멈추는 조기 종료만 있고, 그 효과는 후보 분포에 달려 있다(반경 1km 에 모이면 확정 노드 44%, 흩어지면 95% — `docs/perf.md`) |
| **3주 후반** ✅ | 마감 항목 | ✅ 배차 미상 버스 노선 처리 — 폴백을 없애고 그래프에서 뺐다. "배차 미상 188노선" 은 로그가 **플랫폼 수를 노선 수로 찍은 것**이었고, 실제로 배차가 없는 노선은 **새벽 A 노선 4개**(하루 1회 운행, 플랫폼 188개)다. ✅ 시간대 무운행(`NO_SERVICE`) 반영. ✅ 환승 측정값 교체 + 9호선 보정. ✅ 관리자 화면. ✅ 성능 측정(`docs/perf.md`). ⬜ `maxWalkDistance` 상한(§8 #8) |
| **마무리** | README | 성능 측정 정리. README. 스크린샷·GIF. 여유 시 카카오 API 대조 벤치마크 |

**README는 1주차부터 계속 쓴다.** 수치는 뽑은 즉시 기록한다 — 마지막에 몰아 쓰면 재측정하게 된다.

### D0 — 좌표계 판정 ✅ 완료 (2026-09-14)

구현: [`etl/verify_coords.py`](etl/verify_coords.py) · 리포트: `etl/out/coord_verification.json`

좌표를 아는 지점 8곳(롯데월드타워·코엑스·서울역·고속터미널·63빌딩·IFC·서울시청·잠실종합운동장)의
지번에 입주한 업소 **2,744건**을 후보 좌표계별로 변환해 오차를 실측했다. 표본 전체는 좌표 보유 **505,558건**.

| 후보 | 서울 bbox | 중앙 오차 | 판정 |
|---|---|---|---|
| **EPSG:5174** (PROJ 9.4.1 기본값) | 100.0% | **128.4m** | PASS |
| EPSG:2097 (lon_0 보정 미적용) | 100.0% | 334.8m | FAIL |
| Bessel 보정 + towgs84 7p | 100.0% | 145.0m | PASS |
| Bessel 보정 + towgs84 3p | 100.0% | 123.8m | PASS |
| Bessel 무보정 + towgs84 7p | 100.0% | 349.4m | FAIL |
| *[음성대조군] EPSG:5186* | 0.0% | 100,489.5m | FAIL |

**결론 1 — 원본은 5174 계열이다.** `lon_0 = 127.0028902777778`(보정 적용)이 필수다. 보정을 적용하지 않은 두 후보가 335m·349m로 명확히 갈렸다. 음성 대조군이 100km로 튀어 **검사 자체에 판별력이 있음**도 확인했다.

**결론 2 — datum shift 방식은 무관하다.** 통과한 세 후보끼리의 이격이 **최대 21.3m**로, 앵커 오차(~124m)보다 작다. 어느 쪽을 골라도 결과가 사실상 같다.

**결론 3 — 그래도 proj4 문자열을 명시 고정한다.** EPSG:5174의 datum shift 적용 여부는 **PROJ 버전과 grid 가용성에 따라 달라지고, pyproj(ETL)와 PostGIS(DB)가 서로 다른 정의를 쓸 수 있다.** EPSG 코드에 의존하면 환경에 따라 20m씩 어긋난다. ETL과 Flyway 마이그레이션 양쪽에 동일한 proj4 문자열을 박고, DB 셋업 시 두 결과가 일치하는지 확인한다.

> **잔여 오차 ~124m는 변환 오차가 아니라 앵커 자체의 오차다.** 앵커 좌표는 손으로 넣은 필지 중심값이고 복합건물은 필지가 넓다. 63빌딩이 8.5m, 롯데월드타워가 61.9m인 반면 고속터미널이 206.2m인 것이 그 증거다.
> **전수 검증은 §3의 정류장 정합 검증이 대신했다** — 사람이 넣은 기준값 없이 POI 20,000건 × 정류장 11,494개로 확인했고, 중앙 80.5m가 나왔다.

README에는 이 표와 채택한 proj4 문자열, 잔여 오차의 원인을 그대로 싣는다 — 확인했다는 사실 자체가 증거다.

### D0 요약 — 무엇이 검증됐나

| 항목 | 상태 | 근거 |
|---|---|---|
| POI 원본 좌표계 | ✅ EPSG:5174 계열 | 앵커 8곳 2,744건 대조 · `verify_coords.py` |
| 5186 통일 시 정합 | ✅ 중앙 80.5m | POI 20,000 × 정류장 11,494 · `verify_crs_alignment.py` |
| 버스 구간·순번 | ✅ 인접 정류장 단위, 순번 컬럼 제공 | 574,953행 / 노선당 중앙 58구간 |
| 버스 배차 조인 | ✅ 서울 693노선 100% | 노선번호 → ROUTEID → 노선_ID |
| 지하철 역간 소요시간 | ✅ 유효 98.1%, 중앙 90초 | `TRAIN_NO` 기반 산출 |
| 지하철 시간대별 배차 | ✅ 1~9호선 | 운행현황 RH/NH와 대조 |
| 지하철 역 ↔ 좌표 | ✅ 98.0% | 노선명 매핑 후 (`station_alias`) |
| `Last-Modified`/`ETag` | ✅ 해시로 대체 | 회차마다 SHA-256 을 남기고 같으면 SKIPPED |
| OSM 보행망 추출 | ✅ 서울 전역 | 165,050 노드 / 472,866 엣지 |

### 검수 규칙 — SQL 파일 1개당 1규칙 (`etl/validate/rules/`)
| 규칙 코드 | 내용 | severity | 전체 적재 실측 |
|---|---|---|---|
| `NAME_MISSING` | 상호명 결측 — 검색·표시 불가 | ERROR | 1건 |
| `COORD_MISSING` | 좌표 결측 | ERROR | 39,299건 |
| `STATUS_DATE_CONFLICT` | 폐업인데 폐업일자 없음 / 영업인데 폐업일자 있음 | WARN | 31건 |
| `DATE_LOGIC_ERROR` | 인허가일자 > 폐업일자 | WARN | 0건 |
| `DUPLICATE_NAME_ADDR` | 상호 + 주소 중복 | WARN | 31,782건 |
| `COORD_MOVED` | 직전 회차 대비 좌표 10m 초과 이동 | WARN | (2회차부터) |

- **`COORD_OUT_OF_RANGE`(서울 범위 이탈)는 규칙에서 뺐다.** bbox 는 실제 행정경계가 아니라 넉넉한 사각형이라 개별 건수에 의미가 없다(68만 건 중 6건). 대신 **적재 로그의 계기판**으로만 남긴다 — 좌표 변환이 깨지면 몇 건이 아니라 수십만 건이 밖으로 나가므로, 비율이 1%를 넘을 때만 경고한다
- **`POI_FAR_FROM_TRANSIT` 도 지금은 넣지 않는다.** `transit_stop` 테이블이 선행되어야 해 POI 적재가 정류장 적재에 의존하게 된다. 정류장을 적재한 뒤 유용성을 보고 판단한다
- `NAME_MISSING` 은 **전체 적재에서 실제로 걸려서 추가했다** — `poi.name` 이 NOT NULL 이라 없으면 UPSERT 가 실패한다

ERROR는 serving 반영에서 제외하고 `ingest_run.거절 건수`에 집계한다. WARN은 반영하되 이력만 남긴다.

> **행정동 경계는 쓰지 않는다.** 애초 목적은 ① 좌표 변환 전수 검증 ② 주소-좌표 불일치 검수였는데,
> ①은 §3의 정류장 정합 검증(POI 20,000건, 중앙 80.5m)이 이미 해냈고, ②는 `POI_FAR_FROM_TRANSIT`이 더 잘 한다 —
> 행정동 불일치는 주소 표기 차이(법정동/행정동, 신/구주소)로 **정상인데도 2~3%가 걸리는** 반면,
> 정류장 거리 규칙은 p99가 270m라 **500m 초과가 0.04%(30,000건 중 11건)**로 이상치만 칼같이 분리된다.
> 데이터도 이미 있고 구현도 `verify_crs_alignment.py`에 되어 있다.
>
> PostGIS 근거도 부족하지 않다 — `ST_DWithin`+GiST 반경 검색, `ST_Distance` 스냅, 5174→5186 변환과 그 검증,
> 이 검수 규칙의 공간 조인이 모두 해당한다.

**대상 구역 한정이 필요하면 주소 문자열로 한다.** 자치구 단위 필터에 폴리곤은 과하다. (결국 서울 전역을 그대로 썼다)
```sql
WHERE 지번주소 LIKE '서울특별시 강남구%' OR 지번주소 LIKE '서울특별시 서초구%'
```

### 증분 갱신 — 2층 구조
| 층 | 하는 일 | 얻는 것 |
|---|---|---|
| **① 영업상태 반영** | 새 파일의 영업상태가 폐업이면 `poi.status='CLOSED'` + `closed_date` 기록. **행을 삭제하지 않는다** | 폐업 반영 |
| **② 회차 diff** | 직전 회차 staging과 `FULL OUTER JOIN` → 신규 / 유지 / 소멸(파일에서 사라짐) / 좌표변경 / 상호변경 | `ingest_run`의 신규·유지·소멸 건수 |

- `ingest_run`의 건수 컬럼은 **②에서만 채워진다.** ①만으로는 채울 수 없다
- **소멸(파일에서 사라짐)과 폐업(영업상태 변경)은 다른 사건**이므로 구분해 기록한다
- 삭제하지 않는 이유: 북마크된 POI가 사라지면 방 화면과 계산된 행렬이 깨지고, `includeClosed` 파라미터와 `/admin` 통계가 성립하지 않는다. 검색은 기본 `WHERE status='ACTIVE'`로 거르므로 사용자 경험은 동일하다
- 갱신 판정: `Last-Modified`/`ETag` 조건부 요청 → 변경 없으면 SKIPPED. **헤더가 없으면 파일 해시 비교로 대체**한다(설계 변경 없음)
- 월 1회 전체분(FULL) + 평소 증분(DELTA). 폐업일자·인허가일자 컬럼으로 전체분에서도 증분 추출이 가능하므로 변동분 API 없이 동작한다

### 성능 측정 (README 최상단용)
1. COPY 벌크 로드 vs INSERT 반복 + 인덱스 생성 시점 — **묶어서 1개, 수치만 빠르게** (결론이 교과서라 서사가 약하다)
2. `/places/nearby` — GiST 유무 + **`::geography` 캐스팅 vs 5186 투영 컬럼** EXPLAIN ANALYZE
3. `/places/search` — `LIKE '%q%'` vs **pg_trgm GIN**
4. 그래프 로딩 — DB 매회 로드 vs 인메모리 캐시(`stats.graphSource`), 나아가 **객체 인접리스트 vs CSR(int[]) 표현**

4번이 자격요건 "CS 기본기"에 가장 직접 대응한다.

> **CSR은 이미 들어갔다**(2주 후반): 노드 213,571 · 엣지 607,154 가 약 19MB, 적재 3.3초.
> 남은 것은 **대조군**이다 — `Map<Node, List<Edge>>` 구현을 하나 더 만들어 같은 질의를 돌려야
> 수치가 비교가 된다. 대조군 없는 "CSR을 썼습니다"는 주장이지 측정이 아니다.

### 실수로 얻은 측정값 (README 에 그대로 쓸 것)

의도한 벤치마크는 아니지만 원인과 수치가 분명해 더 설득력이 있다.

| 항목 | before | after | 원인 |
|---|---|---|---|
| staging 적재 | 8분 | 173초 | 트랜잭션 안에서 `ANALYZE` 누락. 자동 통계 수집이 새 행을 못 봐 플래너가 빈 테이블로 취급 |
| ACCESS 스냅 | 8분+ | 수초 | 같은 원인(`graph_node`) |
| 그래프 정리 | 17분+ (취소) | 4.1초 | 외래키 컬럼 인덱스가 **복합 인덱스라 안 탔다** — `(build_id, to_node_id)`는 `WHERE to_node_id = ?`를 못 탄다 |
| 빌드 전체 삭제 | 10분 | 0.9초 | DELETE → TRUNCATE |

### 파이프라인이 "시스템"임을 증명하는 증거
| 증거 | 확인 방법 |
|---|---|
| 멱등성 | 같은 스냅샷 2회 실행 → 회차 테이블 두 행의 신규/유지/소멸 건수 동일 |
| 증분 처리 | 변동분 실행 → 신규·소멸·폐업전환 건수 집계 |
| 변경 감지 | 동일 파일 재실행 → SKIPPED 기록 (조건부 요청 또는 해시) |
| 실패 격리 | 깨진 파일 실행 → FAILED 기록, serving 테이블 무변경 |
| 검수 이력 | `/admin/validation-summary` 응답을 그대로 표로 |

일일 자동 실행을 1주 후반에 걸어두면 남은 2주 동안 회차가 저절로 쌓인다.

### 경로탐색이 맞다는 증거

| 증거 | 확인 방법 |
|---|---|
| **두 구현이 같은 답을 낸다** | `etl/route.py`(파이썬 참조 구현)와 Spring Boot 가 독립적으로 같은 결과 — 잠실→광화문 26분 31초 / 환승 1 / 2호선→5호선, 북한산 좌표→미아 57분 37초 / 도보 2534m |
| 환승이 공짜가 아니다 | 환승 구간 비용 > 대기만 있을 때. 실측 환승 도보가 빠지면 을지로4가에서 2→5호선이 1초에 갈아타진다(실제로 있었던 버그) |
| one-to-many 가 실제로 하나다 | 목표를 5개로 늘려도 확정 노드 수가 가장 먼 목표 하나일 때와 같다 |
| 조기 종료가 먹는다 | 가까운 목표가 먼 목표보다 확정 노드 수가 적다 |
| 시간대가 결과를 바꾼다 | 같은 구간을 8시/22시로 계산하면 소요시간이 다르다 — 아니면 `departureHour` 가 무의미하다 |
| 직결에 환승을 만들지 않는다 | 강남→홍대입구가 2호선 한 노선, 환승 0 |

> **참조 구현을 지우지 않는 이유가 여기 있다.** 서버 하나만 있으면 "맞는 것 같다"까지가 한계다.
> 독립적으로 만든 둘이 같은 답을 내야 검증이 된다.

### 테스트가 통과하는데 동작하지 않았던 것

초록불이 곧 정상은 아니었던 사례들이다. README 에 쓸 만한 소재이기도 하다.

| 무엇 | 왜 안 잡혔나 | 어떻게 잡았나 |
|---|---|---|
| **CSRF 쿠키가 한 번도 안 내려갔다** | 토큰은 누군가 요구해야 만들어진다. 테스트는 `with(csrf())` 로 토큰을 **밀어 넣고** 있어서 발급 경로를 건너뛰었다. 브라우저에서는 모든 POST 가 403 이 될 상태 | 발급부터 직접 하는 테스트를 따로 만들었다. `GET /auth/csrf` 는 그때 생겼다 |
| **`with(csrf())` 가 공유 컨텍스트를 오염시킨다** | 요청에만 작용하는 줄 알았는데 `CsrfFilter` 의 토큰 저장소를 테스트용으로 바꿔 끼운다. 스프링은 컨텍스트를 클래스 간에 재사용한다 | 클래스 단독 실행은 통과, 전체 실행은 실패 — 같은 코드가 순서에 따라 다른 결과를 냈다. `@DirtiesContext` 로 분리 |
| **`gradlew` 가 CRLF 로 체크아웃됐다** | 셔뱅이 `#!/bin/sh\r` 이 되어 도커 빌드가 exit 127. 앞 단계가 `\|\| true` 로 감싸여 있어 같은 실패가 안 보였다 | `.gitattributes` 로 LF 고정 |
| **인덱스를 만들었지만 안 탔다** | `(build_id, to_node_id)` 복합 인덱스로는 `WHERE to_node_id = ?` 를 못 탄다. 만들었다는 사실만 확인하고 **실제로 타는지는 확인하지 않았다** | 빌드 정리가 17분이 지나도 안 끝나면서 드러났다. 단일 컬럼 인덱스로 교체 → 4.1초 |

> 공통점은 **검증 수단이 검증 대상을 건너뛰고 있었다**는 것이다. 테스트를 더 쓰는 것으로는
> 해결되지 않고, "이 테스트가 실제로 무엇을 지나가는가"를 따져야 나온다.

---

## 7. 핵심 스키마 (표 수준)

| 테이블 | 계층 | 핵심 컬럼 | 비고 |
|---|---|---|---|
| `ingest_run` | 운영 | source, snapshot_date, status(RUNNING/SUCCESS/FAILED/SKIPPED), source_etag·source_hash, 원본/유효/거절/**신규/유지/소멸/폐업전환** 건수, 소요시간 | 회차별 한 행. README 표의 원천 |
| `poi_raw` | raw | **surrogate PK만** + run_id + 원본 컬럼 전부 `text` NULL 허용 | 가공하지 않고 보관. **PK·타입 제약을 걸지 않는다** — 원본 중복 1건이 COPY 전체를 죽이면 "실패 격리"가 성립하지 않는다 |
| `poi_staging` | staging | 타입 변환·좌표 변환(5174→5186)·중복 제거 결과, run_id | 검수와 회차 diff의 대상. 직전 회차와 `FULL OUTER JOIN` |
| `validation_result` | 운영 | run_id, rule_code, **severity(ERROR/WARN)**, target_id, detail | 규칙 코드는 §6 표 참조. ERROR는 serving 제외 + 거절 건수 집계 |
| `poi` | serving | **`BIGSERIAL` PK + `UNIQUE(source, source_id)`**, 상호명, 업태구분(정규화 코드 + 원본명), **geom(Point, 5186)**, status(ACTIVE/CLOSED), closed_date, 검수 회차·확인일, first/last_seen_run_id | **API가 읽는 유일한 테이블.** 인허가번호를 PK로 쓰지 않는다 — 원본 키 체계가 바뀌면 PK와 북마크 FK가 함께 깨진다 |
| `category_map` | serving | 원본 업태구분명 → 정규화 코드 | 표기 흔들림 흡수 |
| `transit_stop` | serving | 정류장·역 ID, 명칭, 수단(BUS/SUBWAY), 노선, geom(Point, 5186) | 원본은 4326. **`POI_FAR_FROM_TRANSIT` 검수의 기준 집합**이기도 하다 |
| `station_alias` | serving | 시각표 (호선, 역명) → 역사마스터 (호선, 역사명) | 노선명 매핑 + 수동 9건. 매핑 없이는 70.6%, 있으면 98.0% |
| `app_user` | 서비스 | `login_id`(소문자 정규화, 유니크), `nickname`, `password_hash`(BCrypt), created_at | 테이블명이 `app_user`인 것은 `user`가 SQL 예약어이기 때문. 닉네임 중복은 `lower(nickname)` 유니크 인덱스로 대소문자 무시하고 차단 |
| `room` | 서비스 | **UUID PK**, title, owner_id → `app_user` | 연번이면 남의 방 번호를 추측해 열어볼 수 있다 |
| `room_member` | 서비스 | room_id, user_id, `UNIQUE(room_id, user_id)`, 출발지 라벨·geom(5186)·스냅 결과 + **`origin_build_id`** | 출발지는 **통째로 있거나 통째로 없다**(CHECK). `origin_node_id`에 **외래키를 걸지 않는다** — 아래 참조 |
| `bookmark` | 서비스 | room_id, added_by, poi_id(nullable), **추가 시점 이름·주소·좌표 스냅샷**, geom(5186) | `poi_id`로 담아도 이름·좌표를 복사한다. 방이 기억하는 것은 그 가게가 아니라 **그날 그 자리를 후보로 골랐다는 사실**이다 |
| `graph_node` / `graph_edge` | 그래프 | **`build_id`**, 노드 유형(WALK/STOP/PLATFORM), 엣지 유형(WALK/ACCESS/BOARD/ALIGHT/RIDE/TRANSFER), `weight_sec`, **`weight_by_hour smallint[24]`** | 오프라인 빌드 결과. 시간대별 가중치는 **별도 테이블이 아니라 배열 컬럼**이다 — 아래 참조 |
| `transit_route` | 그래프 | (mode, source_id) → 표시 이름, 노선 유형 | `graph_edge.line`은 **식별자**다(버스는 `100100017`). 식별자를 노선명으로 바꾸면 이름이 겹치는 두 노선이 합쳐져 그래프가 틀어지므로, 이름표를 옆에 둔다 |
| `graph_build` | 운영 | build_id, **`is_active`**, 수단별 노드·엣지 수, 환승 페널티 폴백 비율, 고립 노드 수, 빌드 소요시간 | `/admin/graph-stats`의 원천 |

### 설계 원칙
- **raw / staging / serving 분리** — 원본 재다운로드 없이 가공을 재실행할 수 있다. raw는 제약 없이 받고, 검증은 staging에서 한다
- **serving은 항상 UPSERT.** 테이블 전체 교체(swap)를 쓰지 않는다 — 전체분 재적재가 일 변동분으로 갱신한 영업상태를 덮어쓰기 때문이다. 실패 격리는 **staging에서 검수 통과분만 단일 트랜잭션 MERGE**로 보장한다
- **폐업은 삭제가 아니라 `status` 변경**(소프트 삭제). 소멸(파일에서 사라짐)은 별도 사건으로 기록한다
- **좌표계**: 저장·연산은 5186(미터 단위), API 직렬화 시에만 4326으로 변환
- **그래프는 버전 관리**. 재빌드 중 반쪽 그래프를 읽지 않도록 `is_active`로 전환하고, 멤버 스냅 결과의 `origin_build_id`가 활성 빌드와 다르면 재스냅한다
- **`room_member.origin_node_id`에 외래키를 걸지 않는다.** `graph_node`는 빌드마다 통째로 갈리고 오래된 빌드는 지워진다. 외래키를 걸면 그래프를 새로 만들 때마다 참가자의 출발지가 함께 지워지거나 삭제가 막힌다. 참조 무결성 대신 **`origin_build_id` 비교로 유효성을 판정**한다 — 제약으로 못 지키는 관계는 제약을 거는 대신 명시적으로 확인한다
- **시간대별 가중치는 배열 컬럼**(`weight_by_hour smallint[24]`)이다. 원안은 `graph_edge_weight(edge_id, hour, weight)` 별도 테이블이었는데, 엣지가 60만이면 행이 1,400만을 넘는다. 그래프를 통째로 메모리에 올리므로 **조인할 이유가 없다** — 조인이 없으면 정규화의 이득도 없다
- **외래키 컬럼에는 인덱스를 직접 만든다.** PostgreSQL은 자동으로 만들어 주지 않고, 없으면 CASCADE 삭제가 참조 테이블을 순차 탐색한다(§6 「테스트가 통과하는데 동작하지 않았던 것」 — 빌드 하나 정리에 17분이 걸렸다)

---

## 8. 보완 내용 / 미확인 리스크

### 데이터 소스 교체로 해소된 리스크
| 초안 리스크 | 해소 방법 |
|---|---|
| LOCALDATA 좌표계 EPSG 불명(5174/2097) | **5174 확정** |
| 상가정보↔LOCALDATA 상호명 매칭률 | **대조 구조 폐기** — 인허가 단일 소스로 통일 |
| 상가정보 과거 분기 스냅샷 확보 | **상가정보 제외**로 불필요 |
| TAGO 배차간격 제공 여부 / 활용신청 승인 대기 | **서울시 CSV 실측 데이터로 대체** — 외부 API 불필요 |
| 버스 구간 소요시간 미제공 | **시간대별 실측 데이터 확보** — 추정 모델 폐기 |
| 환승 도보시간 미확보 | **수도권 도시철도 환승 데이터(OA-22521) 측정값** — 1~8호선. 9호선은 아래 「검증되지 않은 가정」 |
| 지하철 배차간격 미확보 | **도시철도 열차운행시각표 확보** — 1~9호선 시간대별 산출 |
| 9호선·타 운영사 노선 제외 우려 | **시각표가 1~9호선 전부 커버** — 9호선 102구간 확보 |
| 행정동 경계 출처 미확정 | **행정동 경계를 쓰지 않기로 결정** — §6 검수 규칙 참조 |

### 착수 전 반드시 확인 (막히면 설계가 바뀜)
| # | 항목 | 못 풀면 |
|---|---|---|
| 1 | ~~5174→5186 변환 오차~~ **해소 (2026-09-14)** — 5174 계열 확정, datum shift 방식은 결과에 무관(이격 최대 21.3m). §6 D0 참조 | — |
| 2 | **`Last-Modified`/`ETag` 제공 여부** — 정부 파일 서버는 헤더를 주지 않거나 매번 200을 반환하는 경우가 흔하다 | 파일 해시 비교로 SKIPPED 판정 (설계 변경 없음) |
| 3 | ~~버스 CSV 정류소 ID 체계 일치~~ **해소 (2026-09-14)** — 구간↔정류장마스터 ID 동일 체계 확인. 서울 노선 좌표 보유 86.2%, 경기·인천은 제외 | — |
| 4 | 실제 파일 갱신 주기 — "최근 폐업일자 2026-09"는 **데이터 내용**이지 갱신 주기의 증거가 아니다 | 며칠 간격 2회 수신 후 해시 비교로 실측. 주기가 길면 README에 실제 주기를 명시 |
| 5 | ~~지하철 배차간격~~ **해소 (2026-09-15)** — 시각표로 1~9호선 시간대별 산출. 버스와 같은 `배차÷2` 공식 | — |
| 6 | ~~지하철 노선 커버리지 차이~~ **해소 (2026-09-15)** — 시각표가 1~9호선 전부 커버. 역명은 `station_alias` 매핑으로 98.0% | — |
| 7 | ~~OSM 보행망 강남·서초 추출~~ **해소 (2026-09-18)** — 구역을 좁힐 필요가 없었다. **서울 전역** 165,050 노드 / 472,866 엣지로 빌드 107초(버스 포함 마감 빌드는 약 150초) | — |
| 8 | **`maxWalkDistance` 상한 미적용** — 계획서는 1200m 인데 아직 안 넣었다. POI 60곳 표본에서 1200m 초과는 1건뿐이고 그 1건도 반경 2.5km 안에 정류장이 없는 정당한 결과였다. 급하지 않지만 명세와 구현이 어긋난 상태다 | 상한을 넣거나, 넣지 않기로 하고 명세에서 뺀다 |

### 검증되지 않은 가정
- 지하철 배차는 **`(역, 방향, 시간대)` 단위**다. 분기 노선(1호선 인천/신창행, 5호선 마천/하남행 등)에서는 목적지에 따라 실제 대기가 더 길 수 있다 — 정확히 다루려면 경로 의존 모델이 필요하고 범위를 넘는다. **README에 명시한다**
- 시각표는 계획 운행시각이다. 지연·감축 운행은 반영되지 않는다
- 시간대별 가중치는 **출발 시각 기준으로 고정**한다 — 경유 중 시간 경과를 반영하지 않는 단순화
- 신분당선·경의중앙선 등 **시각표에 없는 노선은 그래프에서 빠진다**. 신분당선(강남·양재)이 빠지는 것이 가장 아쉬운 부분이며, README에 "1~9호선 기준"을 명시한다
- 카카오 길찾기 API 승인 소요 기간 미확인 — **선택 항목이며 없어도 프로젝트가 완결된다**
- 카카오 로컬 API는 응답 결과의 별도 저장을 허용하지 않음(실시간 호출만) — 저장이 필요한 설계는 피할 것
- API 명세의 필드명·기본값은 설계안 — 실제 데이터 컬럼 확인 후 조정
- ~~`file.localdata.go.kr`는 Referer 헤더를 요구한다. 자동 실행 전 이용약관을 확인할 것~~ **해소 (2026-09-22)** — LOCALDATA 는 2026-04 공공데이터포털로 이전됐고, 포털의 데이터셋 페이지(15045016 등)가 이 파일 서버 직접 다운로드를 제공 방식으로 안내한다. 이용허락범위 제한 없음, 매일 2일 전 기준 갱신, 오픈API 없음. `etl.fetch` 가 사이트 버튼과 같은 순서(다운로드 횟수 확인 → 안내 페이지를 Referer 로 받기)로 서울 파일 두 개를 받는다
- ~~버스 노선 27%(188개)에 배차 데이터가 없다~~ **정정** — 188 은 노선 수가 아니라 빌드 로그가 찍은 **플랫폼 수**였다. 실제로 배차가 없는 노선은 새벽 A148·A160·A504·A741 4개(배차 0, 첫차=막차=03:30)다. 기본 5분 폴백은 **없앴다**. 배차를 모르는 노선에 대기를 지어내지 않고 그래프에서 뺀다
- **9호선 환승은 측정값이 아니라 추정치다.** 측정 파일(4-1)이 9호선 1단계를 전부 10:00 자리값으로 두었다. 거리 파일(4)의 `거리 ÷ 1.2` 에 **보정초**를 더해 쓴다 — 두 파일이 겹치는 1~8호선 90쌍에서 `측정 − 거리÷1.2` 의 중앙값(**+88초**)을 빌드 때 유도한다. 보정 없이 1~8호선만 측정값으로 바꾸면 9호선 환승만 싸게 남아 경로가 9호선으로 쏠린다. 겹치는 쌍 교차검증 오차는 중앙값 28초, 90% 가 80초 안이고, 9호선 보정 환승을 ±80초 흔들어도 역→역 11,670쌍 중 97% 이상이 경로를 유지한다. 가정은 하나 — 9호선의 계단·승강장 고정 비용이 1~8호선과 같다. 노량진 1↔9 는 측정값(7:00)을 쓴다
- **급행↔완행 전환 도보 30초는 잰 값이 아니다.** 0초(공짜 전환 → 급행 분리가 무의미)보다 크고 승강장↔대합실 편도보다 작아야 한다는 경계 안에서 고른 값이다. 가중치에는 여기에 급행 배차 ÷ 2 대기가 더해진다
- **승강장↔대합실 시간은 환승값에서 유도한 값**이다 — 역의 환승 도보 중앙값 ÷ 2 에서 시작해, 대합실로 나갔다 다시 타는 길이 환승 통로보다 싸지 않도록 **모자란 승강장만** 올린다(종로3가 1·5호선 +75초, 고속터미널 7·9호선 +44초). 역 전체에 값 하나를 쓰면 중앙값보다 긴 환승(종로3가 1→5, 326초)이 176초로 갈아타졌다. 편도 = 절반이라는 가정 자체는 측정값이 아니다
- **세션은 서버 메모리에 있다.** 서버를 재시작하면 전원 로그아웃된다. 인스턴스가 하나인 동안은 문제가 없고, 늘리려면 세션 저장소가 따로 필요하다 — 범위 밖

### 의도적으로 제외한 것 (되살리지 말 것)
| 항목 | 제외 이유 |
|---|---|
| 사용자 입력 장소 간 POI 동일성 정합 | JD에서 역산한 기능. 사용자 가치가 없고 자유 입력을 강요하게 됨 |
| 모임 장소 랭킹·추천 | 목적함수 선택을 방어할 근거가 없음. 사용자는 순위가 아니라 각자의 이동시간을 원함 |
| 카카오맵 별점·리뷰 표시 | 로컬 API가 평점·리뷰를 제공하지 않음. place_url 링크로 대체(실시간 호출, 미저장) |
| 도로망 conflation (표준노드링크+OSM 병합) | 접합이 확실히 되지 않는 문제. 두 차례 시도에서 동일한 벽 |
| 차량 경로탐색 | 표준노드링크가 왕복 2차로 이상만 포함 → 골목 목적지 처리 불가 |
| 상가(상권)정보 2소스 대조 검수 | 인허가에 사업자등록번호가 없어 상호명 매칭만 가능 → 매칭률에 검수 축 전체가 걸리는 구조. 단일 소스 + 회차 diff로 대체 |
| 외부 길찾기 API 의존 | 전 데이터를 파일로 통일해 클론 후 바로 실행 가능하게 유지 (대조 벤치마크 용도만 선택적으로) |
| OAuth·소셜 로그인, 이메일 인증, 비밀번호 재설정 | 아이디/비밀번호까지는 설계를 단순하게 만들지만(§1), 여기서부터는 다시 JD와 무관한 시간이다 |
| 세션 저장소 분리(Redis 등) | 인스턴스가 하나다. 늘릴 계획이 없는데 미리 넣을 이유가 없다 |
| time-dependent Dijkstra (경유 중 시간 경과 반영) | `departureHour` 고정만으로 시간대 비교라는 목적이 달성됨. 구현 범위 대비 이득이 작다 |
| 행정동 경계 기반 주소-좌표 검수 | 좌표 전수 검증은 정류장 정합 검증이 대신했고, 개별 이상치는 `POI_FAR_FROM_TRANSIT`이 더 잘 잡는다(오탐 2~3% → 0.04%). 구역 한정은 주소 문자열로 충분 |
| 목적지별 배차 산출 | 분기 노선에서 배차가 2~4배 부풀려진다(1호선 8시대 4.5분 → 13.0분, 실측 3분). `(역, 방향)` 단위가 실측에 가깝다 |

### README 작성 방향
1. 첫 10줄: "공간 데이터 수집 → 검수 → 서비스 API 파이프라인" 한 문단 + 처리·검수·성능 수치 표
2. **"외부 API 키 없이 클론 후 바로 실행된다"를 최상단에** — 전 데이터를 파일로 통일한 결정의 직접적 결과이고, 심사자가 직접 돌려볼 수 있다는 뜻이다
3. 기술 스택 나열은 그 아래
4. 경쟁 서비스(위밋플레이스·야만나) 존재를 명시하고 목적이 다름을 밝힘. **출발 시각대별 비교**는 기성 서비스에 없는 축임을 함께 적는다
5. 데이터 한계를 숨기지 않음 — 영업상태 반영 지연, 5174→5186 변환 오차 실측값, **지하철 배차는 `(역, 방향, 시간대)` 단위라 분기 노선에서 과소추정**, 시각표는 계획 운행시각, 경유 중 시간 경과 미반영, 환승 페널티 폴백 비율, **1~9호선 기준(신분당선 등 제외)**
6. 스크린샷·GIF 필수. **1주차 전반에 첫 스크린샷을 확보**해두고 계속 갱신한다
