# MeetMatrix — 프로젝트 계획서

출발지-목적지 이동시간 행렬 기반 모임 장소 비교 서비스
공간 데이터 수집 · 검수 · 갱신 파이프라인 위에 구축

> 카카오모빌리티 「백엔드 개발자(공간정보 시스템 개발)」 지원용 포트폴리오
> 작성 기준일: 2026-09-14 / 목표 기간: 3주

---

## 1. 프로젝트 개요

### 한 줄 정의
지인들이 방을 만들어 가고 싶은 장소를 모으면, 각자의 출발지에서 후보 장소까지의 이동시간(도보 + 지하철 + 버스)을 계산해 **비교표로 제공**하는 서비스. 그 아래에 **장소 데이터 수집·검수·갱신 파이프라인**을 직접 구축한다.

### 사용자 흐름
방 생성(링크 공유, 닉네임만으로 입장) → 각자 가고 싶은 장소 북마크 → 각자 출발지 입력 → 후보 × 참여자 이동시간 행렬 확인. 순위는 시스템이 정하지 않고, 각 셀의 이동시간·환승·도보거리를 보고 사용자가 판단한다.

### 설계 원칙
| 원칙 | 이유 |
|---|---|
| **랭킹·추천 없음** | 시스템은 계산만 하고 판단은 사용자가 한다. 근거 없는 목적함수를 방어할 필요가 없음 |
| **경로탐색 직접 구현** | API로 대체하면 기성 서비스와 차별점이 0이 되고, JD 대응도 약해짐 |
| **인증·실시간 동기화 없음** | JD 가점 0인데 시간을 가장 많이 먹는 구간. 방 링크 UUID + 닉네임, 폴링으로 대체 |
| **프론트는 최소** | 지도 + 표. 그 이상 금지 |
| **데이터 파이프라인이 메인** | JD 담당업무 3개 중 2개가 여기서 채워짐 |
| **수단 추가에 열린 그래프** | 지하철+도보로 먼저 완결시키고, 버스는 같은 구조에 엣지 유형만 추가 |

### JD 대응 매핑
| JD | 대응 항목 |
|---|---|
| ① 공간정보 데이터 모델링 | raw/staging/serving 3계층, POI·검수이력·그래프(버전 관리)·방/북마크 스키마, 좌표계 설계(5174→5186 저장, 4326 직렬화) |
| ② 수집·검수·가공·배포 백엔드 시스템 | 인허가 데이터 적재 + 검수 규칙 + **회차 간 diff로 신규/유지/소멸 추적** + 조건부 요청 기반 증분 갱신 + 서비스 API |
| ③ 성능 개선 | 벌크 로드, 인덱스 전략, 반경 검색(GiST), 전문 검색(pg_trgm), 그래프 인메모리 캐싱 |
| (자격요건) CS 기본기 | 다중 출발지 최단경로(환승 포함) 직접 구현 |
| (우대) Java/Spring · PostgreSQL 설계 · GIS 개발 | 서비스 레이어 전체 · 3계층 스키마와 공간 인덱스 · PostGIS 공간 연산과 좌표계 변환 |

### 차별점 (위밋플레이스·야만나 대비)
위밋플레이스는 2019년 출시된 성숙한 서비스로, 참석자 위치와 교통편을 입력하면 이동시간·실시간 교통·배차간격을 고려해 중간지점을 산출한다. 겹치는 것은 결과 화면(UX)뿐이다.

- 탐색 공간이 다름: 그쪽은 지도 전체에서 중간지점을 찾고, 이쪽은 **사용자들이 모은 후보를 비교**한다
- 경로 계산과 POI 데이터를 외부에 의존하지 않고 **직접 구현·직접 적재**한다 — 이 프로젝트의 목적 자체가 그 아래층이다
- **출발 시각대별 비교**: 버스 구간 운행시간 실측 데이터가 시간대별이므로 `departureHour`를 바꿔가며 같은 행렬을 비교할 수 있다. 기성 서비스에 없는 축이다

> 면접 예상 질문 "위밋플레이스랑 뭐가 달라요?" 답변: 서비스로 경쟁하려는 게 아니라 그 밑의 데이터 처리와 경로 계산을 직접 구현한 프로젝트다. 문제 정의도 다르다.

---

## 2. 기술 스택

| 구분 | 선택 | 비고 |
|---|---|---|
| 백엔드 | Spring Boot 4 + JDK 25 | 학습 중인 스택. 우대사항 직접 대응 |
| DB 접근 | JdbcTemplate 또는 MyBatis | JPA/Hibernate Spatial 금지 — geometry 매핑에서 며칠 태움 |
| DB | PostgreSQL + PostGIS | GiST 인덱스, pg_trgm, 좌표계 변환(5174→5186), 공간 조인 |
| 마이그레이션 | Flyway | 스키마 변경 이력이 곧 설계 증거 |
| ETL | Python (pandas, GeoPandas, pyproj) | 기존 강점. 여기서 시간 낭비 금지. 좌표 변환은 pyproj로 파라미터를 명시 지정 |
| 프론트 | React + Vite + TS | 지도 1개 + 행렬 표 1개 |
| 지도 | 카카오맵 JS SDK 또는 Mapbox GL JS | 배경지도 표시 용도 |
| 인프라 | Docker Compose (postgis + app) | 재현 가능성 확보 |
| 형상관리 | GitHub 공개 레포 `meet-matrix` | README가 승부처 |
| IDE | 서버 IntelliJ / ETL·프론트 VS Code | SQL 문자열 자동완성·Database 툴 |

쓰지 않는 것: 로그인·OAuth, WebSocket, pgRouting, 외부 길찾기 API(대조 벤치마크 용도 제외).

> **외부 API 키·활용신청이 필요한 구성요소가 없다.** 배경지도만 키가 필요하고, 나머지는 전부 파일이다.

---

## 3. 사용 데이터

| # | 데이터 | 출처 | 용도 | 갱신 | 확인된 사항 / 주의점 |
|---|---|---|---|---|---|
| 1 | 일반음식점·휴게음식점 인허가 | LOCALDATA (행정안전부) | **POI 본체 + 영업상태** | 전체분 + 일 변동분 | 상호명·주소·좌표·업태구분명·영업상태·폐업일자·인허가일자. 좌표계 **EPSG:5174**(보정계수 미적용 Bessel 중부원점TM). `file.localdata.go.kr`는 **Referer 헤더 필요(없으면 403)** |
| 2 | 행정동 경계 | 행정안전부 / 서울 열린데이터광장 | **좌표 변환 전수 검증** + 주소↔좌표 불일치 검수 | - | 5174→5186 변환이 전수 기준으로 맞는지 확인하는 것이 1차 목적 |
| 3 | 도보망 | OpenStreetMap | 도보 구간 | - | OSMnx `network_type='walk'`. **강남·서초 2개 구로 시작** |
| 4 | 지하철 역간 소요시간 | 서울 열린데이터광장 (서울교통공사) | 지하철 승차 구간 | - | 1~8호선. 운행시간(분)·역간거리. **표준 운행시간이며 변동 가능**. 9호선 등 타 운영사 포함 여부 미확인 |
| 5 | 지하철 환승역거리·소요시간 | 서울교통공사 | **환승 페널티 실측값** | - | 역별·환승쌍별 도보 소요시간. 없는 쌍만 기본값 폴백 |
| 6 | 열차운행시각표 | 공공데이터포털 (서울교통공사) | **노선별 평균 배차간격** + (선택) 첫차·막차 | - | 1~9호선. 24시=익일 0시 표기 주의 |
| 7 | 버스 노선 기본정보 | 서울 열린데이터광장 CSV | **배차간격(실측)** | - | 노선별 배차간격 제공 |
| 8 | 버스 구간별 평균 운행시간 | 서울 열린데이터광장 CSV | 버스 승차 구간 | - | **시간대별(1시간 단위) 실측**. 추정 모델 불필요 |
| 9 | 버스 노선·정류소 | 서울 열린데이터광장 CSV | 정류소 좌표·경유 순번 | - | 노선별 경유정류소 목록(순번 포함) |
| 10 | 카카오모빌리티 길찾기 API | 카카오모빌리티 디벨로퍼스 | (선택) 대조 벤치마크 | - | 다중 출발지 길찾기 존재(최대 30개). 권한 신청 절차 있음. **안 써도 프로젝트가 완결된다** |

> **전 데이터가 파일이다.** 외부 API 키·활용신청 없이 클론 후 바로 실행할 수 있다. 심사자가 직접 돌려볼 수 있다는 뜻이며, README 최상단에 쓴다.

### 단일 소스 선택의 이유
초안은 상가(상권)정보를 POI 본체로, 인허가를 대조 검수용으로 쓰는 2소스 구조였다.
그러나 인허가 데이터에 사업자등록번호가 없어 **상호명 매칭만 가능**했고, 매칭률이 낮으면
검수 축 전체가 무너지는 구조적 리스크가 있었다. 인허가 단일 소스로 통일해 이 리스크를 제거하고,
검수는 **단일 소스 내부 규칙 + 회차 간 diff**로 구성한다(§6 2단계).

부수 효과로 POI가 음식점·카페로 한정되는데, 모임 장소 서비스의 후보로는 오히려 적합하다.

### 이동수단 범위: 도보 + 지하철 + 버스
- 차량은 제외 — 표준노드링크가 왕복 2차로 이상 도로만 구축 대상이라 골목 목적지 처리가 안 되고, 서울 지인 모임에서 전원 차량 가정은 비현실적
- **완성 순서는 지하철+도보 → 버스.** 지하철+도보만으로도 프로젝트가 완결되도록 만들고, 버스는 같은 그래프 구조에 노드·엣지를 추가하는 방식으로 확장한다. 일정이 밀리면 버스가 먼저 빠진다

### 버스·지하철 소요시간 — 전부 실측
버스 구간 운행시간과 배차간격을 서울시 CSV에서 실측으로 확보했다. **추정 모델을 쓰지 않는다.**

- 승차 구간: 구간별 평균 운행시간 **시간대별(1시간 단위)** 값
- 대기: 배차간격 ÷ 2 — 버스는 노선별 실측, **지하철도 운행시각표에서 산출한 노선별 평균 배차로 동일 공식 적용**
- 지하철에만 대기시간이 없으면 지하철이 부당하게 유리해지므로, **둘 다 넣거나 둘 다 뺀다**
- 환승 페널티도 서울교통공사 환승역거리·소요시간의 역별 실측값을 쓴다. 폴백 비율은 `/admin/graph-stats`에 노출

### 시간대별 가중치 처리
버스 구간 운행시간이 1시간 단위로 다르므로 정적 엣지 가중치로는 데이터를 쓸 수 없다.

- 버스 엣지는 가중치를 **시간대 배열(24개)**로 보관하고, 탐색 시점에 `weights[departureHour]`를 읽는다. 그래프 빌드는 한 번만 한다
- 도보·지하철 엣지는 시간 불변이므로 단일 값을 유지하되 같은 인터페이스로 감싼다
- 요청 파라미터 `departureHour`(기본 19)로 노출 — **"19시 출발 vs 21시 출발 비교"가 위밋플레이스에 없는 차별점**이 된다
- 단순화: 경유 중 시간 경과는 반영하지 않는다(출발 시각 기준 고정). README에 명시한다

---

## 4. API 명세

### 공통 규약
- Base URL `/api/v1`, JSON, UTF-8
- **요청·응답 좌표는 WGS84(EPSG:4326)** `lng`/`lat`, 시간은 초, 거리는 미터
- **내부 저장은 EPSG:5186**(§7). 4326 변환은 API 직렬화 계층에서만 수행한다
- 인증 없음. 방 접근은 `roomId`(UUID) 소지로, 멤버 식별은 입장 시 발급되는 `memberId`를 헤더 `X-Member-Id`로
  - **`memberId`는 입장 응답에서 본인에게만 1회 발급한다.** 방 상태 응답의 멤버 목록에는 표시용 공개 ID(`publicId`)와 닉네임만 내려보낸다 — 전체 `memberId`를 노출하면 소지만으로 타인의 출발지를 수정할 수 있다
- 에러는 `code`/`message`/`timestamp` 구조. 주요 코드: `INVALID_PARAMETER`(400), `MEMBER_NOT_IN_ROOM`(403), `ROOM_NOT_FOUND`·`POI_NOT_FOUND`(404), `ORIGIN_UNREACHABLE`(409, 출발지 스냅 실패), `MATRIX_NOT_COMPUTABLE`(422, 출발지 또는 후보 0건), `MATRIX_ALREADY_RUNNING`(429, 방 단위 동시 계산 1건 제한)

### 4.1 장소

**GET `/places/search`** — 키워드 POI 검색. 자체 DB만 조회, 외부 API 미호출.

| 파라미터 | 필수 | 설명 |
|---|---|---|
| `q` | O | 상호명 부분 일치 검색어. `LIKE '%q%'`는 B-tree를 못 타므로 **pg_trgm GIN 인덱스** 사용 |
| `lng`, `lat` | X | 주어지면 거리순 정렬 |
| `radius` | X | 반경(m), 기본 2000, 최대 10000 |
| `category` | X | **업태구분 코드**(정규화값). 원본은 인허가의 업태구분명 문자열이며 `category_map`으로 정규화한다 |
| `includeClosed` | X | 폐업 항목 포함 여부, 기본 false. 기본 질의는 `WHERE status='ACTIVE'` |

응답 항목: `poiId`, `name`, `category`(정규화 코드 + 원본 업태구분명), 도로명주소, 좌표, `status`(ACTIVE/CLOSED), `closedDate`, 검수 회차와 확인일, (좌표 준 경우) 거리.

**GET `/places/nearby`** — 좌표 기준 반경 검색. ST_DWithin + GiST 성능 측정의 대상 엔드포인트. 파라미터: `lng`·`lat`(필수), `radius`, `category`. 응답 형식은 search와 동일.
> 내부 질의는 5186 미터 단위로 수행한다(도 단위 오판과 `::geography` 캐스팅으로 인한 인덱스 미사용을 피한다).

**GET `/places/{poiId}`** — 단건 상세. 기본 정보에 더해 데이터 출처(원본 스냅샷 기준일), 검수 결과(적용 규칙·판정·severity·확인일), 등장 회차(first/last seen run), 영업상태 변경 이력을 포함한다.

### 4.2 방

**POST `/rooms`** — 방 생성. 요청: `title`, `nickname`. 응답: `roomId`, 공유 URL, 생성자의 `memberId`.

**POST `/rooms/{roomId}/members`** — 입장. 요청: `nickname`. 응답: `memberId`.

**GET `/rooms/{roomId}`** — 방 상태 전체(멤버 목록·출발지 설정 여부·북마크 목록·`matrixReady` 플래그). 프론트는 이 엔드포인트만 폴링한다.
- 멤버 목록은 `publicId` + 닉네임 + 출발지 설정 여부만 노출한다. **타인의 `memberId`를 포함하지 않는다**
- `matrixReady`는 **"계산 가능 여부"**(출발지 1명 이상 + 북마크 1개 이상)를 뜻한다. 행렬 계산은 동기이므로 "계산 완료 여부"가 아니다

**PUT `/rooms/{roomId}/members/me/origin`** — 자기 출발지 설정. 좌표 직접 지정 또는 지하철역 지정. 응답에 **스냅 결과(연결된 그래프 노드, 스냅 거리, `graphBuildId`)를 항상 포함** — 스냅 거리가 크면 계산 신뢰도가 낮다는 신호이므로 숨기지 않는다. 스냅 실패 시 409.
> 저장된 `graphBuildId`가 활성 빌드와 다르면 행렬 계산 시 **재스냅 후 진행**한다(그래프 재빌드로 node_id가 무효화되는 것을 방어).

### 4.3 북마크

**POST `/rooms/{roomId}/bookmarks`** — 후보 추가. 검색 결과 선택(`poiId`) 또는 지도 클릭 직접 입력(`name` + 좌표) 두 방식. 응답: `bookmarkId`.
> `poiId` 선택 시에도 **추가 시점의 이름·좌표를 북마크 행에 스냅샷으로 복사**한다. 이후 해당 POI가 폐업 처리되거나 원본에서 사라져도 방 화면과 이미 계산된 행렬이 깨지지 않는다.

**DELETE `/rooms/{roomId}/bookmarks/{bookmarkId}`** — 작성자 본인만. 204.

### 4.4 이동시간 행렬 — 핵심 엔드포인트

**POST `/rooms/{roomId}/matrix`** — 모든 멤버 출발지 × 모든 북마크의 이동시간 행렬 계산.

**동기 계산 후 결과를 반환한다.** 2개 구 규모·출발지 5~10명이면 one-to-many 다익스트라 수 회로 충분히 수초 내다. 비동기 잡·폴링을 두지 않는다. 방 단위 동시 계산은 1건으로 제한한다(초과 시 429) — 공개 URL이므로 최소한의 남용 방어가 필요하다.

요청 옵션(전부 선택):

| 옵션 | 기본 | 설명 |
|---|---|---|
| `mode` | TRANSIT | TRANSIT(도보+지하철+버스) / SUBWAY_ONLY / WALK |
| `departureHour` | 19 | 출발 시각대(0~23). **버스 엣지 가중치를 이 시간대 값으로 선택**한다 |
| `transferPenalty` | 실측값 | 환승쌍별 서울교통공사 실측 도보시간. 데이터 없는 쌍만 180초 폴백 |
| `maxWalkDistance` | 1200m | 진출입 도보 허용 최대 거리 |
| `walkSpeed` | 1.2 m/s | 도보 속도 |

응답 구성:

| 블록 | 내용 |
|---|---|
| `origins` / `destinations` | 행렬의 축 (멤버 목록, 북마크 목록) |
| `cells` | 셀별 `duration`, `walkDistance`, `transfers`, `waitTime`, 이용 수단 조합, `reachable`, (실패 시) `unreachableReason` |
| `stats` | `dijkstraRuns`, `expandedNodes`, `elapsedMs`, `graphSource`(CACHE/DB), `graphBuildId`, `transferPenaltyFallbackRatio` |

- **목적지 스냅 실패는 해당 셀만 `reachable=false` + 사유 코드**로 처리하고 행렬 전체는 성공으로 반환한다. 출발지 스냅 실패만 409다
- `departureHour`를 바꿔 같은 방을 두 번 계산하면 버스 이용 셀의 `duration`이 달라진다 — 이것이 시간대 비교 기능이다

> **`stats` 블록이 성능 서사를 담는다.** `dijkstraRuns`가 출발지 수와 같고 목적지 수와 무관하다는 것이 one-to-many 탐색의 증거이고, `expandedNodes`·`elapsedMs`가 최적화 전후 비교 수치가 된다. 랭킹은 제공하지 않는다 — 정렬과 판단은 사용자의 몫.

**GET `/rooms/{roomId}/routes`** — 셀 하나를 눌렀을 때의 단건 경로 상세.
파라미터: `originMemberId`, `bookmarkId`, `mode`, `departureHour`. 응답: 총 소요시간, 구간(leg) 목록 — 도보/지하철/버스/환승 각각의 구간별 소요시간·거리·노선명·정차 수 — 그리고 지도 표시용 LineString 좌표열(4326).

> 좌표를 다시 받아 재계산하지 않고 **행렬과 같은 입력·같은 그래프 빌드에서 재현**한다. 좌표 재입력 방식은 동률 경로에서 행렬 셀과 상세가 어긋날 수 있다.

### 4.5 운영·검수 (읽기 전용 공개)

심사자가 코드를 읽지 않아도 파이프라인 동작을 확인할 수 있게 한다.

| 엔드포인트 | 내용 |
|---|---|
| GET `/admin/ingest-runs` | 적재 회차 목록 — 회차별 상태(SUCCESS/FAILED/SKIPPED), 원본/유효/거절 건수, **신규/유지/소멸/폐업전환 건수**, 소요시간. **README에 붙일 표가 이 응답 그대로** |
| GET `/admin/validation-summary` | 최신(또는 지정) 회차의 검수 규칙별 집계 (severity별 ERROR/WARN 분리) |
| GET `/admin/validation-results` | 개별 판정 레코드 (규칙·회차·severity 필터, 페이징 — 기본 50, 최대 500) |
| GET `/admin/graph-stats` | 그래프 빌드 결과 — `buildId`, 활성 여부, 수단별 노드·엣지 수, 환승 엣지 수, **환승 페널티 폴백 비율**, 고립 노드 수, 빌드 소요시간 |

### 엔드포인트 요약

| Method | Path | 용도 |
|---|---|---|
| GET | `/places/search` · `/places/nearby` · `/places/{poiId}` | POI 검색·반경 조회·상세 |
| POST | `/rooms` | 방 생성 |
| GET | `/rooms/{roomId}` | 방 상태(폴링 대상) |
| POST | `/rooms/{roomId}/members` | 입장 |
| PUT | `/rooms/{roomId}/members/me/origin` | 출발지 설정 |
| POST / DELETE | `/rooms/{roomId}/bookmarks[/{id}]` | 후보 추가·삭제 |
| **POST** | **`/rooms/{roomId}/matrix`** | **이동시간 행렬 계산 (동기)** |
| GET | `/rooms/{roomId}/routes` | 단건 경로 상세 |
| GET | `/admin/*` | 적재 회차·검수 집계·그래프 통계 |

---

## 5. 디렉토리 구조 (상위 수준)

| 경로 | 역할 |
|---|---|
| `etl/ingest/` | LOCALDATA 인허가·행정경계·교통 CSV 적재. 단일 진입점(`python -m ingest --snapshot …`), 조건부 요청, 회차 diff |
| `etl/validate/rules/` | 검수 규칙 — SQL 파일 1개당 1규칙 |
| `etl/graph/` | 도보망 + 지하철 + 버스 → 그래프 스냅샷 빌드 |
| `server/ … /place` | POI 검색·상세 |
| `server/ … /room` | 방·멤버·북마크 |
| `server/ … /routing` | 그래프 도메인, 다익스트라, 캐시, 스냅 |
| `server/ … /matrix` | 다중 출발지 행렬 계산 |
| `server/ … /admin` | 적재·검수·그래프 조회 |
| `server/ … /resources/db/migration` | Flyway 마이그레이션 |
| `web/` | React — 지도, 행렬 표, 장소 검색 |
| `docs/` | api.md, schema.md, validation-rules.md, benchmark.md |

---

## 6. 작업 순서 (3주)

원칙: **얇은 수직 슬라이스를 1주차에 완성**한다. 아래층을 전부 쌓고 위로 올라가면 중간에 막혔을 때 보여줄 것이 없다. 첫 스크린샷을 1주차 전반에 확보한다.

| 시점 | 작업 | 상세 / 산출물 |
|---|---|---|
| **D0** | 사전 검증 | ① **5174→5186 변환 검증**(아래) ② `Last-Modified`/`ETag` 제공 여부와 실제 갱신 주기 확인 ③ 서울시 버스 CSV 4종 스키마 확인 ④ (선택) 카카오 길찾기 API 권한 신청 — **외부 API 승인 대기가 사라져 D0가 짧아졌다** |
| **1주 전반** | 스키마 + 적재 + 첫 화면 | Docker Compose + Flyway. raw/staging/serving 3계층. 인허가 적재(강남·서초). COPY 벌크 로드, GiST 인덱스. `/places/nearby` + **지도에 점 찍는 최소 프론트** → 첫 스크린샷 |
| **1주 후반** | 검수 + 증분 갱신 | 검수 규칙 SQL 5종. **행정동 경계 전수 검증**. **회차 diff**(신규/유지/소멸/폐업전환). 조건부 요청 SKIPPED. 멱등성 확인. **일일 자동 실행 등록** → 이후 2주간 회차가 저절로 쌓인다 |
| **2주 전반** | 그래프 빌드 (도보+지하철) | OSM 보행망 + 지하철을 하나의 그래프로. 노선별 역 노드 복제로 환승 엣지화. **환승 실측값·배차 대기 반영**. 최대 연결요소만 유지. `build_id` 버저닝. 고립 노드 수 기록 |
| **2주 후반** | 서버 골격 + 경로탐색 | Spring Boot. 장소·방·북마크 API. 다익스트라 + 인메모리 캐싱 + 스냅. 단건 경로 동작 |
| **3주 전반** | 행렬 계산 | 출발지별 one-to-many 탐색, 후보 반경 사전 축소, `stats` 채우기. 행렬 표 화면 |
| **3주 후반** | 버스 + 시간대 | 서울시 CSV로 버스 노드·엣지 추가. **시간대 배열 가중치 + `departureHour` 비교 UI**. **유일한 절단 지점** — 밀리면 SUBWAY_ONLY로 완결 |
| **마무리** | 성능 + README | 성능 측정 정리. README. 스크린샷·GIF. 여유 시 카카오 API 대조 벤치마크 |

**README는 1주차부터 계속 쓴다.** 수치는 뽑은 즉시 기록한다 — 마지막에 몰아 쓰면 재측정하게 된다.

### D0 — 5174 → 5186 변환 검증 (최우선)
LOCALDATA 좌표는 보정계수 미적용 Bessel 중부원점TM이다. **EPSG:5174의 `towgs84` 파라미터는 PROJ 버전에 따라 정의가 다르고, 적용 여부로 수십~수백 미터 차이가 난다.** 전체가 같은 방향으로 밀리면 지도에서 눈으로는 잡히지 않는다.

1. 좌표를 아는 지점 5개(서울시청·강남역·홍대입구역 인근 업소)를 데이터에서 찾아 변환 후 실좌표와 대조
2. **오차 50m 이내 → 통과 / 100m 초과 → `towgs84` 처리 오류**
3. 통과해도 **변환 파라미터와 실측 오차를 README에 기재**한다 — 확인했다는 사실 자체가 증거다
4. 전수 검증은 1주 후반의 행정동 경계 조인으로 수행한다

### 검수 규칙 (단일 소스 기준)
| 규칙 코드 | 내용 | severity |
|---|---|---|
| `COORD_MISSING` | 좌표 결측 | ERROR |
| `COORD_OUT_OF_RANGE` | 서울 범위 이탈 | ERROR |
| `ADDR_COORD_MISMATCH` | 주소 문자열의 동 ≠ 좌표가 포함된 행정동 (`ST_Contains`) | WARN |
| `STATUS_DATE_CONFLICT` | 폐업인데 폐업일자 없음 / 영업인데 폐업일자 있음 | WARN |
| `DATE_LOGIC_ERROR` | 인허가일자 > 폐업일자 | WARN |
| `DUPLICATE_NAME_ADDR` | 상호 + 주소 중복 | WARN |
| `COORD_MOVED` | 회차 간 좌표 이동 (diff에서 검출) | WARN |

ERROR는 serving 반영에서 제외하고 `ingest_run.거절 건수`에 집계한다. WARN은 반영하되 이력만 남긴다.

> **행정동 경계의 1차 목적은 좌표 변환 전수 검증이다.** 정상이면 불일치율 2~3%(주소 표기 차이·경계 인접), 변환이 틀렸으면 **30% 이상**으로 급등한다. 이 한 숫자로 판정이 끝난다.

### 증분 갱신 — 2층 구조
| 층 | 하는 일 | 얻는 것 |
|---|---|---|
| **① 영업상태 반영** | 새 파일의 영업상태가 폐업이면 `poi.status='CLOSED'` + `closed_date` 기록. **행을 삭제하지 않는다** | 폐업 반영 |
| **② 회차 diff** | 직전 회차 staging과 `FULL OUTER JOIN` → 신규 / 유지 / 소멸(파일에서 사라짐) / 좌표변경 / 상호변경 | `ingest_run`의 신규·유지·소멸 건수 |

- `ingest_run`의 건수 컬럼은 **②에서만 채워진다.** ①만으로는 채울 수 없다
- **소멸(파일에서 사라짐)과 폐업(영업상태 변경)은 다른 사건**이므로 구분해 기록한다
- 삭제하지 않는 이유: 북마크된 POI가 사라지면 방 화면과 계산된 행렬이 깨지고, `includeClosed` 파라미터와 `/admin` 통계가 성립하지 않는다. 검색은 기본 `WHERE status='ACTIVE'`로 거르므로 사용자 경험은 동일하다
- 갱신 판정: `Last-Modified`/`ETag` 조건부 요청 → 변경 없으면 SKIPPED. **헤더가 없으면 파일 해시 비교로 대체**한다(설계 변경 없음)
- 월 1회 전체분(FULL) + 평소 증분(DELTA). 폐업일자·인허가일자 컬럼으로 전체분에서도 증분 추출이 가능하므로 변동분 API 없이 동작한다

### 성능 측정 (README 최상단용)
1. COPY 벌크 로드 vs INSERT 반복 + 인덱스 생성 시점 — **묶어서 1개, 수치만 빠르게** (결론이 교과서라 서사가 약하다)
2. `/places/nearby` — GiST 유무 + **`::geography` 캐스팅 vs 5186 투영 컬럼** EXPLAIN ANALYZE
3. `/places/search` — `LIKE '%q%'` vs **pg_trgm GIN**
4. 그래프 로딩 — DB 매회 로드 vs 인메모리 캐시(`stats.graphSource`), 나아가 **객체 인접리스트 vs CSR(int[]) 표현**

4번이 자격요건 "CS 기본기"에 가장 직접 대응한다.

### 파이프라인이 "시스템"임을 증명하는 증거
| 증거 | 확인 방법 |
|---|---|
| 멱등성 | 같은 스냅샷 2회 실행 → 회차 테이블 두 행의 신규/유지/소멸 건수 동일 |
| 증분 처리 | 변동분 실행 → 신규·소멸·폐업전환 건수 집계 |
| 변경 감지 | 동일 파일 재실행 → SKIPPED 기록 (조건부 요청 또는 해시) |
| 실패 격리 | 깨진 파일 실행 → FAILED 기록, serving 테이블 무변경 |
| 검수 이력 | `/admin/validation-summary` 응답을 그대로 표로 |

일일 자동 실행을 1주 후반에 걸어두면 남은 2주 동안 회차가 저절로 쌓인다.

---

## 7. 핵심 스키마 (표 수준)

| 테이블 | 계층 | 핵심 컬럼 | 비고 |
|---|---|---|---|
| `ingest_run` | 운영 | source, snapshot_date, status(RUNNING/SUCCESS/FAILED/SKIPPED), source_etag·source_hash, 원본/유효/거절/**신규/유지/소멸/폐업전환** 건수, 소요시간 | 회차별 한 행. README 표의 원천 |
| `poi_raw` | raw | **surrogate PK만** + run_id + 원본 컬럼 전부 `text` NULL 허용 | 가공하지 않고 보관. **PK·타입 제약을 걸지 않는다** — 원본 중복 1건이 COPY 전체를 죽이면 "실패 격리"가 성립하지 않는다 |
| `poi_staging` | staging | 타입 변환·좌표 변환(5174→5186)·중복 제거 결과, run_id | 검수와 회차 diff의 대상. 직전 회차와 `FULL OUTER JOIN` |
| `validation_result` | 운영 | run_id, rule_code, **severity(ERROR/WARN)**, target_id, detail | 규칙 코드는 §6 표 참조. ERROR는 serving 제외 + 거절 건수 집계 |
| `poi` | serving | **`BIGSERIAL` PK + `UNIQUE(source, source_id)`**, 상호명, 업태구분(정규화 코드 + 원본명), **geom(Point, 5186)**, status(ACTIVE/CLOSED), closed_date, 검수 회차·확인일, first/last_seen_run_id | **API가 읽는 유일한 테이블.** 인허가번호를 PK로 쓰지 않는다 — 원본 키 체계가 바뀌면 PK와 북마크 FK가 함께 깨진다 |
| `category_map` | serving | 원본 업태구분명 → 정규화 코드 | 표기 흔들림 흡수 |
| `admin_boundary` | serving | 행정동 코드·명, geom(5186) | 좌표 변환 전수 검증 + 주소-좌표 검수 |
| `room` / `member` / `bookmark` | 서비스 | UUID 키, 닉네임, `public_id`(노출용), 출발지 geom(5186)과 스냅 결과 + **`graph_build_id`**, 북마크(poi_id + **추가 시점 이름·좌표 스냅샷**) | 인증 없음. `member_id`는 본인에게만 발급 |
| `graph_node` / `graph_edge` | 그래프 | **`build_id`**, 노드 유형(보행/역/정류소×노선), 엣지 유형(도보/지하철/버스/환승/진출입), 가중치(초) | 오프라인 빌드 결과 |
| `graph_edge_weight` | 그래프 | edge_id, hour(0~23), weight | **버스 시간대별 가중치.** 도보·지하철은 단일 값 컬럼 사용 |
| `graph_build` | 운영 | build_id, **`is_active`**, 수단별 노드·엣지 수, 환승 페널티 폴백 비율, 고립 노드 수, 빌드 소요시간 | `/admin/graph-stats`의 원천 |

### 설계 원칙
- **raw / staging / serving 분리** — 원본 재다운로드 없이 가공을 재실행할 수 있다. raw는 제약 없이 받고, 검증은 staging에서 한다
- **serving은 항상 UPSERT.** 테이블 전체 교체(swap)를 쓰지 않는다 — 전체분 재적재가 일 변동분으로 갱신한 영업상태를 덮어쓰기 때문이다. 실패 격리는 **staging에서 검수 통과분만 단일 트랜잭션 MERGE**로 보장한다
- **폐업은 삭제가 아니라 `status` 변경**(소프트 삭제). 소멸(파일에서 사라짐)은 별도 사건으로 기록한다
- **좌표계**: 저장·연산은 5186(미터 단위), API 직렬화 시에만 4326으로 변환
- **그래프는 버전 관리**. 재빌드 중 반쪽 그래프를 읽지 않도록 `is_active`로 전환하고, 멤버 스냅 결과의 `graph_build_id`가 활성 빌드와 다르면 재스냅한다

---

## 8. 보완 내용 / 미확인 리스크

### 데이터 소스 교체로 해소된 리스크
| 초안 리스크 | 해소 방법 |
|---|---|
| LOCALDATA 좌표계 EPSG 불명(5174/2097) | **5174 확정** |
| 상가정보↔LOCALDATA 상호명 매칭률 | **대조 구조 폐기** — 인허가 단일 소스로 통일 |
| 상가정보 과거 분기 스냅샷 확보 | **상가정보 제외**로 불필요 |
| TAGO 배차간격 제공 여부 / 활용신청 승인 대기 | **서울시 CSV 실측 데이터로 대체** — 외부 API 불필요 |
| 버스 구간 소요시간 미제공 | **시간대별 실측 데이터 확보** — 추정 모델 폐기 |
| 환승 도보시간 미확보 | **서울교통공사 환승역거리·소요시간 실측값 사용** |

### 착수 전 반드시 확인 (막히면 설계가 바뀜)
| # | 항목 | 못 풀면 |
|---|---|---|
| 1 | **5174→5186 변환 오차** (§6 D0) — PROJ 버전별 `towgs84` 정의 차이로 수십~수백 미터 어긋날 수 있고, 전체가 같이 밀리면 눈으로 안 잡힌다 | 변환 파라미터를 명시 지정하거나 좌표 대신 주소 기반 지오코딩으로 전환 |
| 2 | **`Last-Modified`/`ETag` 제공 여부** — 정부 파일 서버는 헤더를 주지 않거나 매번 200을 반환하는 경우가 흔하다 | 파일 해시 비교로 SKIPPED 판정 (설계 변경 없음) |
| 3 | 서울시 버스 CSV 4종의 정류소 ID 체계가 서로 일치하는지 | 불일치 시 좌표 기반 매칭으로 보완, 실패율을 `/admin/graph-stats`에 노출 |
| 4 | 실제 파일 갱신 주기 — "최근 폐업일자 2026-09"는 **데이터 내용**이지 갱신 주기의 증거가 아니다 | 며칠 간격 2회 수신 후 해시 비교로 실측. 주기가 길면 README에 실제 주기를 명시 |

### 검증되지 않은 가정
- 지하철 역간 소요시간에 9호선·신분당선 등 **타 운영사 노선 포함 여부 미확인**
- 지하철 배차 대기시간은 **운행시각표에서 산출한 노선별 평균** — 시간대별 변동은 반영하지 않는다(버스와 비대칭)
- 시간대별 가중치는 **출발 시각 기준으로 고정**한다 — 경유 중 시간 경과를 반영하지 않는 단순화
- 카카오 길찾기 API 승인 소요 기간 미확인 — **선택 항목이며 없어도 프로젝트가 완결된다**
- 카카오 로컬 API는 응답 결과의 별도 저장을 허용하지 않음(실시간 호출만) — 저장이 필요한 설계는 피할 것
- API 명세의 필드명·기본값은 설계안 — 실제 데이터 컬럼 확인 후 조정
- 2주 전반 그래프 빌드가 최대 리스크 — 보행망 스냅과 고립 노드 처리에서 초과 가능. **대상 구역을 강남·서초 2개 구로 한정**해 방어한다
- `file.localdata.go.kr`는 Referer 헤더를 요구한다(없으면 403). **자동 실행 파이프라인에 넣기 전 이용약관을 확인**할 것

### 의도적으로 제외한 것 (되살리지 말 것)
| 항목 | 제외 이유 |
|---|---|
| 사용자 입력 장소 간 POI 동일성 정합 | JD에서 역산한 기능. 사용자 가치가 없고 자유 입력을 강요하게 됨 |
| 모임 장소 랭킹·추천 | 목적함수 선택을 방어할 근거가 없음. 사용자는 순위가 아니라 각자의 이동시간을 원함 |
| 카카오맵 별점·리뷰 표시 | 로컬 API가 평점·리뷰를 제공하지 않음. place_url 링크로 대체(실시간 호출, 미저장) |
| 도로망 conflation (표준노드링크+OSM 병합) | 접합이 확실히 되지 않는 문제. 두 차례 시도에서 동일한 벽 |
| 차량 경로탐색 | 표준노드링크가 왕복 2차로 이상만 포함 → 골목 목적지 처리 불가 |
| 상가(상권)정보 2소스 대조 검수 | 인허가에 사업자등록번호가 없어 상호명 매칭만 가능 → 매칭률에 검수 축 전체가 걸리는 구조. 단일 소스 + 회차 diff로 대체 |
| 외부 길찾기 API 의존 | 전 데이터를 파일로 통일해 클론 후 바로 실행 가능하게 유지 (대조 벤치마크 용도만 선택적으로) |
| time-dependent Dijkstra (경유 중 시간 경과 반영) | `departureHour` 고정만으로 시간대 비교라는 목적이 달성됨. 구현 범위 대비 이득이 작다 |

### README 작성 방향
1. 첫 10줄: "공간 데이터 수집 → 검수 → 서비스 API 파이프라인" 한 문단 + 처리·검수·성능 수치 표
2. **"외부 API 키 없이 클론 후 바로 실행된다"를 최상단에** — 전 데이터를 파일로 통일한 결정의 직접적 결과이고, 심사자가 직접 돌려볼 수 있다는 뜻이다
3. 기술 스택 나열은 그 아래
4. 경쟁 서비스(위밋플레이스·야만나) 존재를 명시하고 목적이 다름을 밝힘. **출발 시각대별 비교**는 기성 서비스에 없는 축임을 함께 적는다
5. 데이터 한계를 숨기지 않음 — 영업상태 반영 지연, 5174→5186 변환 오차 실측값, 지하철 배차는 노선 평균, 경유 중 시간 경과 미반영, 환승 페널티 폴백 비율
6. 스크린샷·GIF 필수. **1주차 전반에 첫 스크린샷을 확보**해두고 계속 갱신한다
