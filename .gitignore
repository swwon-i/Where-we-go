# 원본 데이터 — 용량이 커 저장소에 올리지 않는다 (일반음식점 154MB, 버스 구간속도 141MB)
# 출처와 수신 방법은 계획서 §3 참조. ETL이 이 경로를 읽는다.
csv/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.playwright-mcp

# Java / Gradle
build/
.gradle/
*.class

# Node
node_modules/
dist/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# 카카오맵 JavaScript 키 (개발용). 발급: developers.kakao.com
server/.env

# Gradle 빌드 산출물 (wrapper 는 커밋한다)
server/build/
server/.gradle/

# 프론트 빌드 결과 — web/ 이 소스이고 Docker 가 빌드한다
server/src/main/resources/static/

# 적재 로그 — DB 의 ingest_run 이 기록의 원본이다
etl/out/logs/

# 산출 결과 — 스크립트로 언제든 다시 만든다
etl/out/subway/

# OSM 보행망 — 191MB. osmnx 로 언제든 다시 받는다
etl/out/graph/

# osmnx 가 Overpass 응답을 캐싱하는 곳
cache/
